---
name: project-backend-refactor-arc
description: "Progress + next steps for the DDD/CQRS \"pragmatic spine\" back-end refactor arc (TODO"
metadata: 
  node_type: memory
  type: project
  originSessionId: e24ce96e-656e-429c-bb6b-16afa01f73d9
---

The pragmatic-spine DDD/CQRS refactor (TODO #55, design in `docs/BACKEND-REFACTOR-DESIGN.md`, conventions C1–C12). Each PR: rich aggregate (invariants on the entity) + `BookTracker.Application` command handlers + thin hand-rolled `IDispatcher` (convention-scan registration, no MediatR); ViewModels inject `IDispatcher` and dispatch, signatures unchanged so Razor/dialogs are untouched.

**Done (merged):**
- PR1 Book aggregate pilot · PR2 Work (ref-count C11) · PR3 Series (#362/#363) · PR4 Wishlist (#364) · PR5 Merge ops as commands (#367 — Book/Work/Author/Edition `Merge*` command handlers; merge services trimmed to read loaders; shared `AuthorMergeCompatibility`).
- **PR6a — read-model relocation (pushed 2026-06-26, `feat/read-models`).** Established the **read side**: `IQuery<T>`/`IQueryHandler<,>`/`IDispatcher.Query` (mirrors command side, same convention scan). Relocated to `Application`: both snapshots → `Application/Catalog/` (`GetCatalogSnapshot`/`GetWishlistSnapshot`; `BuildInfo` stays host-side, passed in as `Version`), the 4 merge loaders → `Get*MergePreview` query handlers (merge VMs now **dispatcher-only**), and `WorkAuthorshipFormatter`+`SeriesOrderParser` → `Application/Formatting/` (~18 callers). Read handlers use `AsNoTracking()`+DTO (C5). Tests → `Application/<Feature>/*HandlerTests`. High-effort review: 0 correctness defects (1 maintainability finding → **TD-16** cover-pick dup; stale-using nit fixed).

**Remaining — PR6b (display VMs → off DbContext), then close-out.** Drew chose **Option B** (finish each VM off DbContext: reads→queries AND stray writes→commands) in **~4 grouped sub-PRs**, each branched from fresh `main`:
- **6b-1 Dashboard+simple lists — DONE (pushed/merged 2026-06-26, `feat/read-models-displayvm-1`).** Home/Series/AuthorList read-only → `GetHomeDashboard`/`GetSeriesList`/`GetAuthorList` query handlers; the 3 VMs are now dispatcher-only (kept presentation logic: Home maxima, Series badge helpers+enum parse, AuthorList filter). Test-split shape established (= the template for 6b-2…4): DB-backed assertions → `Application/<Feature>/Get*HandlerTests`, VM keeps thin `IDispatcher`-mock unit tests. **Correction to the original plan:** did *not* extract a shared author-rollup query — Home (per-WorkAuthor-contribution tally), AuthorList (distinct works/books/series), and the snapshot (`AuthorSnapshot.BookCount`, distinct books) are **three deliberately-different metrics**; unifying would change Home's output, so kept separate and logged as **TD-17** (the in-app `GetAuthorList` altitude + 3 "same-shape, different-metric" duplications) to consolidate behind one query *at arc close*, not mid-arc. High-effort review: 0 correctness bugs, 4 maintainability findings → all folded into TD-17.
- **6b-2 Author-admin + Publisher — DONE + REVIEWED (`244a6b0`+`2e31add` on `feat/read-models-displayvm-2`; review resumed/ran 2026-06-27, awaiting Drew's push).** First 6b sub-PR with **writes**. **High-effort review (8 finders → 16 candidates → 5 survived verify): 1 real UX bug fixed — `AuthorDetailViewModel.Apply` only ever *set* a message channel, never cleared the opposite, so a failed action's red error rendered next to a later success's green alert (Detail.razor shows both); fixed to own both channels + regression test. 1 cleanup fixed — dead `.Include`s in `GetPublisherEditions` before its `.Select`. 3 `GetAuthorDetail` query-shape findings (copy-graph hydrated to count, double Book read, cartesian Include explosion) → all verbatim relocations, logged to TD-17 (e/f/g), deferred to arc-close. 10 candidates refuted as pure-style/no-observable-effect.** Reads → `GetAuthorDetail` (null=NotFound), `GetPublisherList`/`GetPublisherEditions`. Writes → `RenameAuthor`/`MarkAuthorAsAliasOf`/`PromoteAuthorToCanonical` (share `AuthorAdminResult` = Success + toast/error text) and `RenamePublisher`/`DeleteUnusedPublisher`/`MergePublishers` (share `PublisherAdminResult` = `Changed` flag + single `Message` channel; Merge transactional, mirrors PR5). Each write command **lifted verbatim** from the VM method (guards preserved: name-clash leaves row unchanged, alias re-root + re-point priors, edition-reassign-then-delete in one tx). VMs keep only presentation state (Works/Books toggle enum, publisher expand set + lazy detail cache, toast strings, dispatch guards). **Pattern note for 6b-3/4:** the test-split now also covers writes — `Application/<Feature>/*CommandsTests` assert DB-state + result, VM tests are thin `IDispatcher` mocks over guard/cache/toast wiring. Also relocated `PartialDate`/`PartialDateParser` → `Application/Formatting/` (3rd formatter to land there after PR6a's two; 6 caller usings + 1 test). 55 affected tests green, clean `--no-incremental` build.
- **6b-3 BookDetail — DONE + REVIEWED (`9e13b82`+`b5623a0` on `feat/read-models-displayvm-3`; reviewed 2026-06-27, awaiting Drew's push).** `GetBookDetail` (6-Include AsSplitQuery + the BookDetail/WorkDetail/EditionDetail/CopyDetail/TagDetail/SeriesInfo records moved out of the VM into `Application/Books`) + `GetTagSuggestions`; `AddTagToBook`/`RemoveTagFromBook` via a new shared **`TagResolver`**. **BookDetailViewModel dropped `IDbContextFactory` entirely — it's now fully off DbContext** (presentation state + `IDispatcher` only). `BookFormatExtensions.DisplayName` relocated Web→`Application/Formatting` (4th formatter there) so the read handler can project it; `_Imports` already carried the namespace so Razor consumers were untouched. **TagResolver chosen SIMPLE (plain find-or-create, no unique-index-catch — Drew's call, consistent with siblings), so TD-15 only PARTIALLY closes:** the Tag *dedup* half is done (all 3 tag find-or-create sites — AddTagToBook, MarkWishlistItemBought, BulkAddViewModel — route through TagResolver), but the **race** (catch+re-query across all 3 resolvers) + the **Publisher/Series** inline creates in the not-yet-migrated Add-flow VMs remain → TD-15 stays Open, re-scoped in `docs/TECH-DEBT.md`. **Test choice (deviation from 6b-2):** kept BookDetailViewModelTests as integration coverage (the heavy incrementally-migrated VM; its suite now drives the relocated handlers via the real dispatcher AND covers VM-computed props) rather than re-cutting to thin mocks + handler tests; only the new TagResolver got dedicated tests. **High-effort review (8 finders→17 candidates→3 survived): 1 real regression caught + fixed — the optimistic tag chip rendered the lowercased input instead of the resolved row's stored Name, so a tag matching a pre-existing mixed-case row flipped casing on reload (fixed: AddTagToBook returns TagDetail with the stored name; regression test added). 1 cleanup fixed — extracted `TagResolver.Normalize` so resolver+GetTagSuggestions share one rule (the "single owner" doc claim was false). 1 logged to TD-17(h) — GetBookDetail is a 4th load-then-project handler.**
- **6b-4 BookList — DONE + REVIEWED (`b01221a`+`1c44986` on `feat/read-models-displayvm-4`; reviewed 2026-06-27, awaiting Drew's push).** Reads relocated to `Application/Books` (GetLibraryFilterOptions / GetLibraryBooks / GetLibraryGroups + shared `LibraryBookQuery.Filtered`); VM dropped `IDbContextFactory` (fully off DbContext). Enum/records/NoneKey/PageSize moved to `Application/Books`; ripple into List.razor + both BookList test files handled. TD-6 → Resolved (GroupBySelected shadow + 2 sync sites deleted, MudSelect binds VM.SelectedGroupBy directly; 3 grouping tails → one ToGroupRows). TD-2 cartesian preserved verbatim, deferred to TD-17. 682 tests green. **High-effort review (8 finders → 10 candidates → 2 survived, 0 confirmed bugs): F1 fixed — extracted `LibraryFilter.IsSpecificSeries` so the series-reading-order rule isn't expressed in two projects (the sort trigger in the handler + ShowingFlatList in the VM); F2 (sequential awaits in InitializeAsync) skipped — InitializeAsync is test-only, the real page parallelises via two lifecycle hooks. NOTE: TD-6a (MudSelect direct binding) is Razor, not unit-tested — grouping LOGIC is covered by the 22 integration tests but the binding wants a manual sanity-check before final sign-off.** Drew chose to **SPLIT** BookList out from Wishlist (combined ~25+ files = blows out his "unless it blows massively" bar; arc is now **5 sub-PRs**). Both VMs are **read-only migrations now — all writes already dispatch** (BookList: SetBookStatus/MarkBookRead/RateBook; the only behaviour-touch is TD-6a). Reads→queries in `Application/Books`: `GetLibraryFilterOptions` (←LoadFilterOptionsAsync), `GetLibraryBooks(LibraryFilter, sort, page)` (←LoadBooksAsync+ApplyFilters), `GetLibraryGroups(LibraryFilter, groupBy)` (←LoadGroupsAsync + the GroupByAuthor/Genre sub-methods). A `LibraryFilter` record carries VM filter state down; `ApplyFilters`+`BookQueryWithIncludes` lift **verbatim** (Drew chose relocate-verbatim → **TD-2 cartesian/SQL-projection deferred to TD-17 close-out**, NOT fixed here). DTOs (`BookListItem`/`GroupRow`/`GenreOption`/`TagOption`/`SeriesOption`) → `Application/Books`. **Pay down TD-6** (the point of touching BookList): (a) delete `List.razor`'s `GroupBySelected` shadow field + 3 sync sites, bind MudSelect to `VM.SelectedGroupBy`; (b) collapse the 3 grouping tails into one `ToGroupRows` helper → mark TD-6 Shipped. Tests: keep `BookListViewModelTests` (22 integration, drive handlers via dispatcher) + `BookListViewModelQueryStateTests` (10 unit, URL ser/deser, untouched) per the 6b-3 choice; add targeted tests for the new `ToGroupRows`/grouping shaping only. ~10-12 files; TD-6a is behaviour-touching so verify grouping manually (Razor isn't unit-tested).
- **6b-5 Wishlist — DONE + merged (#374, `feat/read-models-displayvm-5`).** Verbatim read relocation: `GetWishlist`/`GetSeriesGaps` (the slot-occupancy math, reuses `SeriesSlots.OccupiesNumberedSlot`)/`GetWishlistDuplicate` in `Application/Wishlist`; DTOs (WishlistRow/SeriesGap/OwnedSeriesBook/OpenSeries) moved, `WishlistCandidate`+`QuickAddInput` stay Web, VM dropped `IDbContextFactory`. Existing 26 integration tests (dispatcher-driven) lock the gaps math end-to-end → no dedicated handler test added. Review: 0 findings. **All 5 6b sub-PRs merged — every display VM is now off DbContext.**

**Survey finding (2026-06-26):** 3 "display" VMs still did **direct DbContext writes** that slipped past PR1–5 — BookDetail tags, AuthorDetail admin, PublisherList admin (Publisher.Merge is transactional). That's why 6b is Option B not reads-only. None of the VM read DTOs leak EF entities (good). VMs already injecting `IDispatcher`: BookList/BookDetail/Wishlist; the other 5 don't yet. **As of 6b-3 the whole trio is migrated — AuthorDetail + PublisherList (6b-2) and BookDetail tags (6b-3) admin writes all route through commands; no display VM does direct DbContext writes anymore.**

**Close-out — DONE (2026-06-27). Arc complete.** Drew's sequencing call: pay down arc-relevant TD *before* the whole-arc review, so the review sees the FINAL state (not deferred items) AND the novel consolidation code (which steps off the verbatim discipline) is itself in the reviewed scope. Scope = **arc surface** (TD-17 + TD-2 + TD-16 + F3; NOT TD-15 race or Mobile TD-7). Scale-audit done manually (no skill). Phased, each branched from fresh `main`, per-PR review on each except the last (whose review IS the whole-arc review):
- **Close-1 — shared rollups — DONE + merged (`refactor/backend-arc-close-1-rollups`).** `AuthorRollups` (one per-author distinct work/book/series count — *originally* `Select(pair).Distinct().ToList()` SQL dedup + in-memory group-count since EF can't translate `Distinct().GroupBy()`; **superseded in close-2b review #2 by per-author correlated `COUNT(DISTINCT)` subqueries** that compute the count in SQL — see Close-2b; canonical = in-memory SUM of members) + `GenreReads.TopGenresAsync` now back /authors, snapshot, Home top-authors, AI prompt — **TD-17 b/c/d consolidated**. **Key decisions (Drew, see [[feedback_light_reads_custom_projections]]):** display-count semantics may CONVERGE (Home top-authors → distinct-books, was contribution-tally) and the canonical rollup DROPS the cross-member DISTINCT — a book credited to both a canonical and an alias counts once *per member* (1 book in ~2000 = King/Bachman omnibus — accepted). Review: F1 fixed (Home could surface a "0 books" author after soft-delete → `Where(BookCount>0)` guard + regression test); F4 (ByCanonical+ByAuthor double-scan) resolved *by* the sum simplification; F3 (BookCount-only callers still run the works+series scans) → TD-17 residue.
- **Close-2a — detail-page projections — DONE + pushed (CI as of 2026-06-27), `refactor/backend-arc-close-2a-detail-reads`.** `GetAuthorDetail` (e/f/g) + `GetBookDetail` (h) convert load-then-project Include graphs → SQL projections (lightweight intermediates + C# formatters over the materialised data; EF Core 10 translates the nested collection projections + `Editions.Sum(e=>e.Copies.Count)`). Behaviour-identical (AuthorDetail+BookDetail integration suites lock it). Review: **0 correctness bugs** (the 3 scary candidates — flat-series null deref, Sum-of-Count translation, redundant sort key — all refuted). F1 fixed (centralised the contributor sort in a new `WorkAuthorshipFormatter.Display((Name,Role,Order))` overload — `Display(Work)` + `GetBookDetail` both route through it). F2 (lead-author pick `WorkAuthors.Where(Role==Author).OrderBy(Order).First()` duplicated 6+ sites) → **TD-18** (cross-cutting, spans SQL+in-memory+non-arc files, own PR).
- **Close-2b — DONE + whole-arc-reviewed + review-fixes applied (pushed 2026-06-27, awaiting CI/merge; `refactor/backend-arc-close-2b-library-scale`).** The cleaner half: **TD-2** — stripped the confirmed-dead `Include`s from `LibraryBookQuery.Filtered` + projected the flat-list page to `BookListItem` in SQL (`GetLibraryBooks`); **TD-16** — shared `Books.BookCovers.PickAsync(IQueryable<Book>)` (single-Work-cover-else-any, both merge previews route through it); **F3** — `AuthorRollups.PerAuthorBookCountAsync` BookCount-only path for Home+snapshot; FK-index review folded in. **Whole-arc high-effort review (8 finders → verify): 6 findings.** Drew's call: #1 accept, fix #2/#3/#4/#6 (commit `7d2605c`). **#1 (accepted):** canonical counts sum per crediting member (the close-1 imprecision). **#2/#5 (fixed — the headline):** close-1's `AuthorRollups` had settled for materialising ~3.6k (author,book) pairs + in-memory group-count *because* EF Core 10 won't translate `Distinct().GroupBy()` OR `GroupBy().Select(g=>g.Distinct().Count())`; the review flagged that bulk transfer as a hot-path (Home+snapshot) scale regression. **Fix: rewrote both `PerAuthorAsync` + `PerAuthorBookCountAsync` as per-author correlated `COUNT(DISTINCT)` subqueries** (`a.WorkAuthors.Where(Role==Author).SelectMany(wa=>wa.Work.Books).Select(b=>b.Id).Distinct().Count()`) — one row per author, the count computed in SQL. **This is the exact shape the old snapshot `directCounts` used — it translates where the group-by doesn't** (needs the `Author.WorkAuthors` nav for the role filter). The books-by-author definition is spelled once per method, kept identical (#5). **#3 (fixed):** stale snapshot comment claimed canonical counts "de-duped so a book credited to both counts once" → corrected to summed-per-member. **#4 (fixed):** `GetHomeDashboard` hit `db.Authors` twice (membership + name lookup) → folded Name into the single membership query, name lookup built in memory. **#6 (fixed):** extracted `AuthorRollups.SelectForDisplay<TVal>` — the one canonical-vs-alias pick rule (canonical→rolled-up total, alias→own) shared by /authors + snapshot, replacing two divergent inline copies. 683 tests green, clean `--no-incremental` (Application 0 warnings). **2b was the LAST code change — arc is now code-complete.**
- **Close-3 — DONE (2026-06-27, this `chore/backend-arc-close-out` PR).** Whole-arc retro [[retro_backend_refactor_arc]] written; `ARCHITECTURE.md` updated (Application/CQRS-spine section + solution structure + Services relocation notes); TODO #55 moved Open→Shipped; `docs/TECH-DEBT.md` TD-2/TD-16/TD-17 → Resolved (TD-6 already Resolved in 6b-4; F3 residue resolved via `PerAuthorBookCountAsync`; TD-18 stays Open as its own PR; TD-15 + C7 deferred); close-out mega-blog shipped (`blog/2026-06-27-01-refactor-that-wasnt-broken.md`). This branch is the batched `chore(memory)` close PR carrying all accrued arc memory.

**Process notes:** per-PR review before push on PR3/4/5/6a/6b-1/6b-2 (all 0 reachable bugs through 6b-1; method [[feedback_review_agents_need_diff_file]] — materialise diff to `.debug/pr<N>.diff` first, gate [[feedback_review_findings_gate]]). Recurring fixes: aggregate factories not raw `new` (C12); centralise duplicated predicates ([[feedback_overloaded_field_shared_predicate]]) — but watch the "looks-duplicated, is-actually-different-metric" trap (TD-17, see [[blog_backend_arc_raw_material]]). Pilot lessons [[retro_backend_ddd_pilot]]. Tech-debt ledger `docs/TECH-DEBT.md` (TD-15 TagResolver lands in 6b-3; TD-16 cover-pick + TD-6 grouping are 6b-adjacent; **TD-17** read-model rollup/dup cluster → consolidate at arc close).
