# TODO

Outstanding work items for BookTracker. Priority-ordered: **security items first** (they always go to the top regardless of size), then the rest sorted by **increasing size / complexity** so small wins are at the top of the non-security list. This file is the single source of truth — code-level `// TODO` comments provide local context but this list is the master.

Size legend: **XS** ≈ one-line fix, **S** ≈ one short session, **M** ≈ a day or a tight multi-PR series, **L** ≈ a multi-day arc or broad cross-cutting work, **XL** ≈ blocked / needs external change.

## Open

| # | Category | Name | Description | Size |
|---|---|---|---|---|
| 1 | Infra / Security | Bacpac sync from prod → staging | Empty staging catches schema syntax errors but **misses every data-shape failure**: `NOT NULL` against existing nulls, unique-index/constraint additions where data has duplicates, FK additions where data has orphans, CHECK violations, type-conversion failures, perf-on-real-data timeouts. EF migrations on SQL Server are transactional so prod can't *corrupt* — but the deploy still fails after merge. Add an on-demand prod → staging bacpac sync (sibling of the existing `infra/refresh-local-db.ps1` for prod → local) — either a `refresh-staging-db.ps1` script or a GH Action workflow (`sync-staging-data.yml`) so it's a one-click pre-flight before any review-tag migration. Workflow path is preferred because it doesn't need SqlPackage on the dev box and runs from inside the VNet. | S |
| 2 | Meta | `security-audit` skill (pilot for audit-skill chassis) | User-level read-only Claude Code skill that flags security issues across code, infra (Bicep), CI/CD (GH Actions), and secrets/config. Produces a dual-audience Markdown report: structured frontmatter + stable rule IDs (`SEC-001`…) + `file:line` citations for LLM consumption, plus a narrative executive summary + severity grouping for human scanning. Skill logic lives at `~/.claude/skills/security-audit/`; project-specific rules at `.claude/audit-rules/security.md` override/extend `default-rules.md`. Templates ship for web-app / SaMD / game / library / CLI so the skill is useful on first run in any project. Reports land in `audits/` (gitignored). **Pilot for the shared chassis** — if the report format, rule-file layout, and handoff contract land well, follow-ons reuse them: `scale-audit` (#21), `a11y-audit` (#10), `codehealth-audit` (#11). Out of scope for v1: `extends:` / `suppress:` rule-pack syntax (copy-paste templates for now), `/init-audits` scaffolder skill, plugin packaging for cross-machine distribution. | M |
| 3 | Security | Nonce-based CSP middleware | Replace the current meta-tag CSP's `'unsafe-inline'` / `'unsafe-eval'` on `script-src` with a per-request nonce threaded through Blazor's framework inlines. The meta-tag CSP added in the initial audit (SECURITY-AUDIT.md §4) is a baseline — this is the tightening. Real XSS hardening; moderate implementation work because Blazor Server's script injection points need to read the nonce. | M |
| 4 | Duplicates | PR 6 — audit "add Edition by ISBN" gap on Edit Book | Audit first, possibly drop. Partial implementation exists via `BookEditViewModel.NewEditionLookupIsbn`; only open a PR if the audit shows a real gap. | S |
| 5 | Infra | Validate Trove ISBN lookup end-to-end | Once the NLA API key arrives, drop into `appsettings.Development.json`, retry a self-published ISBN the other providers miss (e.g. `9780645840407`), confirm DTO parse matches Trove's live v3 response. Follow-up PR if the shape differs. | S |
| 6 | Series | Context help tips in UI | Explain the difference between a "Series" (numbered, known order like The Ender's Game Saga) and a "Collection" (loose grouping like Discworld / Hercule Poirot). Probably small tooltips or helper text on the series picker. | S |
| 7 | Series | Revisit Collection ordering | Currently defaults to publication order (`Series.cs`). Revisit once enough Collection data is captured to see what the right default actually is. | S |
| 8 | Blog | Cross-publish blog posts | Currently posts live only in the repo's `blog/` directory — great for the experiment-frame but audience is limited to people who find the repo. Once there are 2–3 posts to cross-publish, pick a syndication target (Dev.to, Medium, personal site) and decide canonical-URL strategy. Low urgency; keep writing first. | S |
| 9 | Meta | Package reusable Claude prompts into a `prompts/` directory | Two reusable paste-into-another-Claude prompts were extracted during development (git-history-rewrite, going-public walkthrough). Text is saved locally by Drew in separate files but not in this repo. Add a `prompts/` directory at repo root, one `.md` per prompt, with a small `prompts/README.md` explaining the "reusable-instructions-as-deliverable" shape (see `retros/retro_reusable_instructions.md`). Link from the main README. More prompts will accrue over time — set up the pattern once, populate as it grows. | S |
| 10 | Meta | `a11y-audit` skill | Follow-on audit skill reusing the chassis from `security-audit` (#2): WCAG form labels, colour contrast, touch-target sizes, responsive breakpoints on mobile-prioritised pages. Tight scope — start narrow on mobile + a11y fundamentals, expand only if the pilot chassis proves out. Contingent on #2 validating the shared report format + rule-file layout. | S |
| 11 | Meta | `codehealth-audit` skill | Follow-on audit skill reusing the chassis from `security-audit` (#2): dead code, TODO density, test-coverage gaps, `ARCHITECTURE.md` drift vs reality. Contingent on #2 validating the shared report format + rule-file layout. | S |
| 12 | Observability | Health monitoring + perf budgets + capacity alerts | App Insights is wired but there's no synthetic monitoring, no perf budgets, and no resource alerts. Surfaced concretely by the staging-DB-sep deploy: a Bicep redeploy + slot restart + first migrate-on-startup briefly stacked on the S1 ASP + Basic SQL (5 DTU) combo — login timeouts that *looked* like an outage, recovered after a Stop/Start. Without alerts the next time prod is briefly degraded we won't have data showing it self-resolved vs persisted. Four pieces, each can ship as a separate PR: (a) **synthetic heartbeat** — GH Action cron every 15min hitting `/manifest.webmanifest` (Easy Auth excludedPath, no auth needed) + alerting on >2 consecutive failures; (b) **page-load smoke** — PR-time test against staging hitting Home / `/books` / `/books/{id}` with p95 budgets that fail the PR if breached; (c) **resource alerts** — Azure Monitor on SQL DTU > 80% sustained 5min, App Service CPU > 80% sustained 5min, App Insights server response p95 > 3s; (d) **migration timing observability** — log `Database.Migrate()` duration as a custom App Insights event so a slow migration is visible without surprise (today migrate-on-startup is opaque). Sized M for the four moving parts. | M |
| 13 | App | Proper error handling | Structured logging, correlation IDs, user-friendly messages by category, separate 404 handling (`Program.cs:50`, `Error.razor`). Security-adjacent — make sure no stack traces or internals leak in prod error responses. | M |
| 14 | AI | Cost tracking | Persistent token/cost logging beyond the session counter. | M |
| 15 | UI / UX | Shelf-order view | Show books in series order (within each series/collection) or first-publish-date order, so the list mirrors the physical shelf. Probably a sort option on the Library page or a dedicated shelf-planner view. | M |
| 16 | AI | AI-assisted genre matching | Use `IAIAssistantService.SuggestGenresAsync` to classify a book against the preset taxonomy, replacing/augmenting the rule-based `FuzzyGenreMatch` in `BulkAddViewModel.SaveBookAsync`. Higher accuracy, ~$0.0003/book on Sonnet, ~1s per book. Useful for genuinely ambiguous subject strings. | M |
| 17 | Series | Multiple authors on anthologies | "The Best Science Fiction of the Year" has different editors per volume. Per-book authors carry the detail for now; use "Various Authors" or leave blank (`Series.cs`). | M |
| 18 | Series | Multi-series membership | A Discworld novel could belong to both "Discworld" and "Discworld: City Watch" sub-series. Currently one series per book (`Series.cs`). | M |
| 19 | Series | API enrichment for series detection | Open Library has series data that could auto-suggest series membership during ISBN lookup (`Series.cs`). | M |
| 20 | UI / UX | UI testing approach | Evaluate bUnit (component-level) and/or Playwright (browser-level) for testing screens and views. Currently ViewModels are well-tested but Razor markup is not. | M |
| 21 | Meta | `scale-audit` skill | Follow-on audit skill reusing the chassis from `security-audit` (#2): N+1 queries, unbounded `ToListAsync`, missing `Include`, index coverage (FKs, filtered-unique ISBN), `DbContextFactory` discipline (flag any `AddDbContext` regression or injected `DbContext`), Azure SQL Basic tier headroom vs observed query patterns. Enforces the 3000+ copies performance rule as a first-class audit. Contingent on #2 validating the shared report format + rule-file layout. | M |
| 22 | UI / UX | Accessibility review | Screen reader support, keyboard nav, ARIA labels, colour contrast, focus management. Broad sweep across every page. | L |
| 23 | Data model | Short stories / novellas as first-class entities | Some stories appear standalone *and* inside collected-works books. Requires a Story entity (Title, Author, FirstPublished) + many-to-many to Book. Worth modelling only if you actually want to query "which book contains story X" or "all stories by author Y". **Note:** the Work refactor partially addresses this — Work IS that abstract creative unit, and a Book can contain many Works. Defer until the use case is concrete. | L |
| 24 | Infra | Replace migrate-on-startup with deploy-time migrations | Use `dotnet ef migrations bundle` from GitHub Actions once the app goes multi-instance or needs zero-downtime deploys (`Program.cs:39`). Currently migrate-on-startup is simple + safe for the single-instance App Service. | L |
| 25 | Meta | Claude-driven PR review for external contributions | Experiment extension. Once there's actual external interest, set up a GitHub Action that runs Claude Code on every non-author PR: Claude reviews the change, posts review comments + suggested rewrites, tags the human for the accept/reject decision. Continues the "every line of code goes through a Claude session" frame to external work too. **Blocked:** needs a collaborator or external PR traffic that justifies the setup cost + clarity on CI cost + review-quality bounds. | L |
| 26 | AI / Infra | Re-add Microsoft Foundry (Claude on Azure) | Brings back `claude-sonnet-4-6` + `claude-opus-4-7` deployments, the Foundry Private Endpoint, and the `cognitiveservices.azure.com` DNS zone. **Blocked:** current subscription is Sponsored, which MS excludes from Claude on Foundry. Needs EA / MCA-E migration first. Direct Anthropic API works in the meantime. | XL |

### Notes on Open items

- **Duplicate detection** is conservative (exact-after-normalisation). If the noise/signal ratio is wrong in practice, add fuzzy matching (Levenshtein / token similarity) as a follow-up — don't plumb confidence scores until there's a reason for it.
- **MudBlazor rollout** is ongoing via "convert as we touch" (see `ARCHITECTURE.md` > UI component library). Not a scheduled TODO — each page converts when it's substantively edited for other reasons.

## Shipped

| # | Category | Name | Description | Estimate | Actual |
|---|---|---|---|---|---|
| — | Infra / Security | Separate staging database from production | Prod and staging slots now talk to separate Azure SQL databases (`booktracker` + `booktracker-staging`) on the same SQL server. `DefaultConnection` is slot-sticky so swaps stay code-shaped; each slot's managed identity is granted only on its own DB; `deploy.ps1` drops orphan staging-identity grants from the prod DB to keep the isolation clean. First deploy: empty staging DB, migrate-on-startup builds the schema. **Surprise post-deploy:** Bicep redeploy + slot restart + first migrate-on-startup briefly stacked on the S1 ASP + Basic SQL (5 DTU) combo — both slots looked dead with `Connection reset by peer` during AAD login (~6min after deploy.ps1 finished), recovered after a full Stop/Start of each slot. Surfaced new TODO #12 (synthetic monitoring + capacity alerts) so the next degraded-but-self-resolving event is visible. | M | 1 PR (#129) |
| — | Security | GitHub Environment + 2-min wait timer for swap.yml | Set up the `production` GitHub Environment with a 2-minute wait timer (fat-finger cancellation window) and Deployment branches restricted to `main` only. Wired `swap.yml` via `environment: production` on the swap job. Reviewer-approval stays future, blocked on having a collaborator (TODO #25). | S | 1 PR (#124) |
| — | UI / UX | PWA `mobile-web-app-capable` meta tag | Added `<meta name="mobile-web-app-capable" content="yes">` alongside the apple-specific one in `Components/App.razor`. Clears the Chrome deprecation warning while keeping Safari install-to-home-screen working. | XS | 1 PR (#123) |
| — | UI / UX | Manage publishers page | MudBlazor page at `/publishers` mirroring `/authors` structurally — per-row drill-down to editions, inline rename, two-step-confirm merge (no alias model — outright absorption), delete-unused (guarded to zero-edition rows by the `Edition.PublisherId` Restrict cascade). `PublisherListViewModel` holds ops inline matching `AuthorListViewModel`; merge runs in an explicit transaction matching `AuthorMergeService`. | M | 1 PR (#121) |
| — | Blog | Initial blog post — "Why the most-edited part of our codebase isn't code" | First post in the AI-collaboration blog series. Tour of the `.claude-memory/` directory (feedback / user / project / reference types), argues persistent context is first-class engineering work, four rules for durable memory files. Written by Claude, reviewed + approved by Drew — matches the repo's AI-first framing. Triggered a new rule in `feedback_memory_commits.md` about when memory should land (arc-close / external reference / monthly), because 46 untracked memory files had accumulated and the post referenced retros that hadn't been pushed yet. | M | 2 PRs (#117 memory-landing prerequisite, #118 the post itself) |
| — | Meta | Plan + evaluate + flip repo to public | `GOING-PUBLIC.md` captured the decision (single public repo vs split — decided single), stranger's-eyes re-scrub of infra, and the `.claude-memory/` exposure decision (Option A — keep all tracked; workflow-preference files are a portfolio positive). Followed by README-from-scratch landing page with the experiment framing up front, then the actual visibility flip in GitHub Settings. Unblocked the GitHub Environments TODO (since shipped — see above); added a far-future TODO for Claude-driven PR review on external contributions (now #25). | S | 2 PRs (#115 planning doc, #116 README + pre-flip polish) |
| — | Security | Easy Auth client secret rotation | Scheduled twice-yearly rotation via GitHub Actions using the existing booktracker-ci OIDC SP. Keeps latest 2 passwords alive to cover KV reference refresh. 2-year secret lifetime kept as fail-safe. Granted the CI SP KV Secrets Officer, owner of Library-Patrons, Graph Application.ReadWrite.OwnedBy. | M | 1 PR (#111 — bundled feature + fix for Graph app-role grant permission/error handling) |
| — | Security | Security audit (multi-area walkthrough) | Full security posture review: git history secret scan (gitleaks), Easy Auth excludedPaths, CSP + security headers, SignalR auth, KV access paths, dependency vulns, PII in logs, SQL injection surface, JS interop XSS, Azure RBAC scoping, HSTS + HTTPS. Delivered `SECURITY-AUDIT.md` as a living doc + `.github/workflows/security-scan.yml` (gitleaks on PRs + weekly + monthly issue-opening review prompt) + baseline CSP meta tag + X-Frame-Options/X-Content-Type-Options/Referrer-Policy headers. All areas passed; one new open TODO (#3, nonce-based CSP tightening). | L | 1 PR |
| — | UI / UX | MudBlazor pilot + warm library theme | Pilot on Home + MergeBook, custom "leather/brass/parchment/espresso" palette, opt-in rollout across subsequent pages. | M | 2 PRs (#93 pilot+theme, #94 ARCHITECTURE doc) |
| — | UI / UX | Book detail (View) page arc | /books/{id} replaces /edit as the default browsing surface: read-only scaffold → inline auto-save (rating/status/notes/tags) → modal edits for Book + Work → Edition + Copy modals → Library list swap. | L | 6 PRs (#95, #97, #98, #99, #100, + early genre taxonomy #101) |
| — | UI / UX | MudBlazor genre picker | Typeahead + chips + collapsible MudTreeView; wired into WorkEditDialog. Shipped alongside non-fiction taxonomy expansion (Reference/Art/Religion). | M | 2 PRs (#101 taxonomy, #102 picker) |
| — | UI / UX | Authors page drill-down rewrite | MudBlazor rewrite of /authors with per-row Works/Books toggle, alias rollup on canonical rows, deep-link from Home top-10. | M | 2 PRs (#107 rewrite, #109 scroll-into-view polish) |
| — | UI / UX | Mobile UX polish bundle | Hamburger auto-close on nav + clickable Home stat tiles. | S | 1 PR (#106) |
| — | UI / UX | Progressive Web App | Installable on mobile + desktop via manifest + icons + service worker. Network-first-with-cache-fallback for static assets; `/_blazor/*` passes through. Icons reproducible via `scripts/generate-pwa-icons.ps1`. | M | 2 PRs (#91 install, #92 auth-exclusions fix) |
| — | Duplicates | Duplicate management arc (PRs 2–5) | Author merge, Work merge + attach-existing-Work, Edition merge + auto-fill-empties + cover retrofit, Book merge. Detection + listing predates this list. | L | 4 PRs (#87, #88, #89, #90) |
| — | Bugs | Bug nest — double-click, slot-sticky AI, shopping NRE | Dialog double-click guard; Bicep always-emit KV refs + slot-sticky; Shopping ISBN-match NRE (missing `.ThenInclude(w => w.Author)`). | S each | 3 PRs (#103, #104, #105) |
| — | AI | AI book recommendations | Multi-provider (Anthropic direct + Azure OpenAI + Microsoft Foundry in code) with runtime picker. Genre cleanup, collection cataloguing, shopping suggestions, book advisor. | L | ~17 PRs (#45–#61) |
| — | UI / UX | Wishlist UI | Integrated into Shopping page as the shopping-list section. | S | 1 PR |

### Notes on Shipped items

- Priority numbers are omitted for shipped rows — the `#` column is a placeholder. If we start planning shipped work retrospectively (e.g. for post-merge review), priority can go back in.
- The retro for each shipped arc lives under `.claude-memory/retros/` and is the canonical source for surprises/lessons beyond the one-line summary above.
