# TODO

Outstanding work items for BookTracker. Priority-ordered: **security items first** (they always go to the top regardless of size), then the rest sorted by **increasing size / complexity** so small wins are at the top of the non-security list. This file is the single source of truth — code-level `// TODO` comments provide local context but this list is the master.

Size legend: **XS** ≈ one-line fix, **S** ≈ one short session, **M** ≈ a day or a tight multi-PR series, **L** ≈ a multi-day arc or broad cross-cutting work, **XL** ≈ blocked / needs external change.

## Open

| # | Category | Name | Description | Size |
|---|---|---|---|---|
| 1 | Infra / Security | Bacpac sync from prod → staging | Empty staging catches schema syntax errors but **misses every data-shape failure**: `NOT NULL` against existing nulls, unique-index/constraint additions where data has duplicates, FK additions where data has orphans, CHECK violations, type-conversion failures, perf-on-real-data timeouts. EF migrations on SQL Server are transactional so prod can't *corrupt* — but the deploy still fails after merge. Add an on-demand prod → staging bacpac sync (sibling of the existing `infra/refresh-local-db.ps1` for prod → local) — either a `refresh-staging-db.ps1` script or a GH Action workflow (`sync-staging-data.yml`) so it's a one-click pre-flight before any review-tag migration. Workflow path is preferred because it doesn't need SqlPackage on the dev box and runs from inside the VNet. | S |
| 2 | Security | Nonce-based CSP middleware | Replace the current meta-tag CSP's `'unsafe-inline'` / `'unsafe-eval'` on `script-src` with a per-request nonce threaded through Blazor's framework inlines. The meta-tag CSP added in the initial audit (SECURITY-AUDIT.md §4) is a baseline — this is the tightening. Real XSS hardening; moderate implementation work because Blazor Server's script injection points need to read the nonce. | M |
| 3 | Series | Revisit Collection ordering — non-integer SeriesOrder | Surgical fix shipped (`AuthorListViewModel.LoadDetailAsync` + `BookListViewModel.LoadGroupBooksAsync` now honour `Work.SeriesOrder` when present — see Shipped row). What remains is the modelling question: today `int? Work.SeriesOrder` can't represent "5.5" interquels (Stormlight #4.5 *Edgedancer*) or potential "where's my cow" Discworld interludes — Open Library's parser preserves the raw publisher string but leaves `SeriesOrder` null when non-integer, sinking those Works to the bottom of the cluster via `int.MaxValue`. Schema option (mirrored from #14): keep `int? SeriesOrder` for gap-detection sort + add `string? SeriesOrderDisplay` for the original publisher string. UI shows the display string; sort still works on the int. **Defer until** real capture surfaces enough cases to inform the design — overlaps materially with #14 and may merge into it. | M |
| 4 | Meta | Package reusable Claude prompts into a `prompts/` directory | Two reusable paste-into-another-Claude prompts were extracted during development (git-history-rewrite, going-public walkthrough). Text is saved locally by Drew in separate files but not in this repo. Add a `prompts/` directory at repo root, one `.md` per prompt, with a small `prompts/README.md` explaining the "reusable-instructions-as-deliverable" shape (see `retros/retro_reusable_instructions.md`). Link from the main README. More prompts will accrue over time — set up the pattern once, populate as it grows. **Note:** a "skill chassis for guided procedures" (mutating workflows like the going-public walkthrough) is a different shape from the audit-skill chassis (read-only analysis) and would warrant its own pilot if the prompts list grows past 3–4 candidates. For now, the simple `prompts/` directory is the right answer. | S |
| 5 | Meta | `a11y-audit` skill | Follow-on audit skill reusing the chassis from the now-shipped `security-audit` skill: WCAG form labels, colour contrast, touch-target sizes, responsive breakpoints on mobile-prioritised pages. Tight scope — start narrow on mobile + a11y fundamentals, expand only if the chassis pattern proves out beyond security. | S |
| 6 | Meta | `codehealth-audit` skill | Follow-on audit skill reusing the chassis from the now-shipped `security-audit` skill: dead code, TODO density, test-coverage gaps, `ARCHITECTURE.md` drift vs reality. | S |
| 36 | Meta | `style-audit` skill | Follow-on audit skill reusing the chassis from `security-audit` + `scale-audit`. Checks: themed-colour-only (no inline hex outside the canonical palette in `docs/STYLE-GUIDE.md` §Palette), scale-typography-only (no off-rung FontSize values), palette synchronisation between Web (`BookTracker.Web/Theme/BookTrackerTheme.cs`) and Mobile (the MAUI XAML hex literals it currently hand-mirrors), reference-cleanliness (every hex literal in code mapping to a STYLE-GUIDE.md entry). Per the security-audit chassis lesson (`retros/retro_security_audit_skill_poc.md`), defer until at least one second-project style-guide instance exists so the chassis claim earns its place. Pairs with TODO #39. | M |
| 37 | Mobile | Centralise Mobile palette + dark-mode coverage | Two coupled gaps surfaced by `docs/STYLE-GUIDE.md` §Known inconsistencies. (a) **Single source of truth:** MAUI hex literals are hand-mirrored from `BookTrackerTheme.cs` — no language-level enforcement that Mobile stays in sync. Add `Resources/Styles/Colors.xaml` ResourceDictionary declaring each palette colour as `<Color x:Key="Leather">#6B2737</Color>` so XAML can use `{StaticResource Leather}` instead of inline hex. Mirrored from Web's theme by hand still (true single SOT requires codegen — out of scope), but localised. (b) **Dark-mode coverage:** `AppThemeBinding` is applied to ScanPage result cards (`Pages/ScanPage.xaml:40-41`) but not to MainPage's palette. Switching the device to dark mode renders MainPage's light palette against system dark chrome — visually clashes. Same fix surface — ResourceDictionary with light + dark variants, `AppThemeBinding` referencing both. Pairs naturally. | M |
| 38 | UI / UX | Resolve icon-vs-app palette inconsistency (Bookcase side) | **Half resolved 2026-05-14**: Bookshelf MAUI launcher rebranded to the in-app palette (espresso bg `#3E2723` + brass spine `#A67B3A` + parchment text lines `#F2EADB`) so its icon and the running app are visually continuous AND visually distinct from the Bookcase PWA. **Bookcase PWA remains on Material You purple** (`#6750A4` / `#4F378B`) — tapping the purple icon still lands on a parchment-and-leather screen. Three resolution paths in `docs/STYLE-GUIDE.md` §Known inconsistencies (now scoped to Bookcase only): (1) **Rebrand the Bookcase icon to the leather palette** — touches `BookTracker.Web/wwwroot/icons/icon.svg`, the PNG renders via `scripts/generate-pwa-icons.ps1`, `wwwroot/manifest.webmanifest` `theme_color`, `Components/App.razor` `theme-color` meta tag. (2) **Adopt the icon purple in-app** — invert the choice; wider blast radius across every component reference + STYLE-GUIDE.md rewrite, and would walk back Bookshelf's just-shipped alignment. (3) **Accept and document (current state)** — purple is Bookcase brand-mark only; parchment-and-leather is the product palette. Many apps do this. Status: (3) is the default; revisit if real-use confusion surfaces. | S (Bookcase side only — was M when both apps were in scope) |
| 39 | Meta | Cross-project style-guide chassis extraction | Mirror of the audit-skill chassis pattern (one-stack instance → second-stack validation → chassis extraction). Step 1 captured: `docs/STYLE-GUIDE.md` is the BookTracker instance. Step 2 is applying the same structure to a second project — when one materialises, copy STYLE-GUIDE.md, prune BookTracker-specific palette + file:line refs, document what stayed structural (section headings, "Known inconsistencies" framing, table shapes) vs what was instance content. Step 3 is extracting the structural skeleton to `~/.claude/templates/style-guide-template.md` so the next project starts with the right shape — same shape as TODO #4 (reusable Claude prompts) but for design-system docs rather than agent prompts. **Blocked** until project 2 exists with enough UI surface to make a style guide useful. Pairs with TODO #36 (style-audit skill needs at least one second-project instance to earn the chassis claim). | M |
| 42 | Observability | Migrate Application Insights AspNetCore 2.x → 3.x (OpenTelemetry) | Dependabot keeps offering 3.x of `Microsoft.ApplicationInsights.AspNetCore` (currently #222, ignored via `.github/dependabot.yml`'s major-bump rule for this package). 3.x is a full OpenTelemetry-based rewrite of the classic SDK — `ITelemetryInitializer` is removed; the entire concept is replaced with OpenTelemetry processors / enrichers. Direct breaking surface in our code: `BookTracker.Web/Telemetry/UserTelemetryInitializer.cs` (stamps Easy-Auth user name onto every telemetry item) implements `ITelemetryInitializer` and would not compile. Migration approach (when picked up): (a) replace `UserTelemetryInitializer` with a `BaseProcessor<Activity>` registered via `IServiceCollection.ConfigureOpenTelemetryTracerProvider(...)` that reads `IHttpContextAccessor` and sets the `enduser.id` attribute on each Activity; (b) audit `Program.cs` AppInsights setup for the new builder shape (no more `services.AddApplicationInsightsTelemetry()` — the new SDK wires up via OpenTelemetry's hosting extensions); (c) verify the App Service codeless attach path still works alongside the new SDK, or pick one. Minor / patch bumps within 2.x continue to auto-apply. Triggered by 2026-05-12 Dependabot resolution session. | M |
| 48 | Mobile | Bookshelf Wishlist surface | Original sub-piece of #35 (Series gaps shipped 2026-05-14 with title search, this row split out for a fresh planning pass). **Backend shipped** — `/api/wishlist-snapshot` endpoint (`BookTracker.Web/Api/WishlistEndpoints.cs` + `WishlistSnapshotService` + `WishlistSnapshot` DTO under `BookTracker.Shared/Wishlist/`) is live; what remains is the **Bookshelf-side surface only**. Backend shape (decided 2026-05-13, now built): Bookcase remains the canonical write surface (add/remove/edit wishlist rows happen there); Bookshelf reads the wishlist via the parallel `/api/wishlist-snapshot` endpoint (cleaner separation from the catalog snapshot than folding it in), and adds a local SQLite `WishlistBoughtLocal` table keyed by wishlist row ID. Tapping "bought" writes a local row → hides the item from the in-store list. Every catalog refresh clears the local-bought table — so if Drew captured the book via Bookcase the row is server-side gone (never reappears), and if he didn't (damaged copy, changed mind) the row pops back onto the next refresh. Self-healing without a sync protocol for user gestures. **UX shape deferred** until Drew has used #41 (title search) + #35b (series gaps) in real bookstores — the right answer for the Bookshelf wishlist surface depends on how the existing browse-and-find buttons feel under real use. Open questions: separate MainPage CTA (button count goes 6 → 7), or fold into a "Browse" submenu page alongside title search / author search / series gaps; gesture for the "bought" toggle (tap the card / long-press / swipe-right / explicit button); whether the Bookshelf surface is worth doing at all (Bookcase web works fine when not in-store, and series gaps already covers the "what am I missing right now" case). Parallels #28 (consolidate /shopping into /bookshop on Web) — both surfaces would consume the same enriched DTO; consider doing the Web-side enrichment once and consuming from both. | S–M (after planning) |
| 7 | Observability | Health monitoring + perf budgets + capacity alerts | App Insights is wired but there's no synthetic monitoring, no perf budgets, and no resource alerts. Surfaced concretely by the staging-DB-sep deploy: a Bicep redeploy + slot restart + first migrate-on-startup briefly stacked on the S1 ASP + Basic SQL (5 DTU) combo — login timeouts that *looked* like an outage, recovered after a Stop/Start. Without alerts the next time prod is briefly degraded we won't have data showing it self-resolved vs persisted. Four pieces, each can ship as a separate PR: (a) **synthetic heartbeat** — GH Action cron every 15min hitting `/manifest.webmanifest` (Easy Auth excludedPath, no auth needed) + alerting on >2 consecutive failures; (b) **page-load smoke** — PR-time test against staging hitting Home / `/books` / `/books/{id}` with p95 budgets that fail the PR if breached; (c) **resource alerts** — Azure Monitor on SQL DTU > 80% sustained 5min, App Service CPU > 80% sustained 5min, App Insights server response p95 > 3s; (d) **migration timing observability** — log `Database.Migrate()` duration as a custom App Insights event so a slow migration is visible without surprise (today migrate-on-startup is opaque). Sized M for the four moving parts. | M |
| 8 | AI | Cost tracking | Persistent token/cost logging beyond the session counter. | M |
| 9 | UI / UX | Shelf-order view + Shelf concept | Two coupled pieces that landed in the same row during the 2026-05-16 genre-restructure planning. **(a) Shelf-order view (original scope):** show books in series order (within each series/collection) or first-publish-date order, so the list mirrors the physical shelf. Probably a sort option on the Library page or a dedicated shelf-planner view. **(b) Shelf concept (folded in 2026-05-16):** organise physical shelves by broad category, with Terry Pratchett and H.P. Lovecraft carved out as their own shelves. Two implementation options to pick between when this is picked up — **(i) Tag convention:** seed `shelf:*` tags (`shelf:sci-fi`, `shelf:crime`, `shelf:horror`, `shelf:fantasy`, `shelf:pratchett`, `shelf:lovecraft`, `shelf:thriller`, `shelf:romance`, `shelf:general`, `shelf:nonfic`), document convention in `DATA-DICTIONARY.md` §Tag (exactly one `shelf:*` per Book, convention-enforced), add a Shelf filter to Books list — zero schema change, recommended starting point; or **(ii) Shelf entity:** add `Book.ShelfId` FK + `Shelf` table immediately — more upfront work, cleaner long-term if shelving is a permanent organising axis. Decisions already locked from the 2026-05-16 planning regardless of which option ships: Adventure folds into `shelf:thriller` (no standalone `shelf:adventure`); the literary/general-fiction shelf is `shelf:general` not `shelf:drama` (no parallel Genre-side change needed — the existing `Literary Fiction` top-level genre covers the "general novel" case, decision 2026-05-17). | M |
| 10 | UI / UX | In-app help, hints, and onboarding walkthrough | As the app's surface area grows it gets harder for users (Drew first; future users second) to discover what the app can do without reading the README. Three pieces, each shippable as its own PR: (a) **first-run walkthrough** — short guided tour highlighting key surfaces (Library, Add Book, Bulk Add, `/authors`, `/publishers`, `/assistant`) on first sign-in; dismissable per-user (DB flag, not cookie, so it survives device changes); (b) **contextual help** — `?` button on key forms (Series/Edit, Book Add, Bulk Add) opening a brief MudDialog with examples and the deeper version of the helper-text-already-present; (c) `/help` page or about-the-app dialog as the catch-all reference linking to the README + key concepts. Each piece sized small individually; the M is for the trio + the discoverability-pattern decisions (where do `?` icons live, what's the visual treatment, persistence model). Pairs naturally with #11 (the user manual is the source content this can render). | M |
| 11 | Docs / UX | User manual — narrative feature reference | Exploratory testing keeps surfacing forgotten features as BookTracker's surface area grows (Library, Add Book, Bulk Add, `/authors`, `/publishers`, `/assistant`, `/shopping`, `/series`, `/duplicates`, AI Assistant). Need a readable artefact that describes what the app does and where each capability lives — distinct from #10 (interactive walkthrough + contextual help, which is the *in-app discoverability layer*). The manual is the *narrative reference*. **Recommended approach (option iii below):** single markdown source at `docs/USER-GUIDE.md` (editable in IDE, GitHub-renderable, version-controlled) rendered in-app via Markdig at a `/manual` route. Single source of truth feeds both the on-disk docs and the in-app reference; later cross-links to / from #10(c)'s `/help` page when that ships. **Alternatives considered:** (i) **markdown only** in `docs/USER-GUIDE.md` — simplest, no in-app rendering; requires going to GitHub or opening the file in an editor to read. (ii) **hand-coded `/manual` Razor page** — no markdown layer; every content edit becomes a code change. (iii) **markdown source + Markdig-rendered `/manual` page** — single source of truth, in-app + on-disk coverage, small dep add. (iv) **auto-generated route catalogue** — introspect `@page` directives + their docstrings to build the reference; stays in sync with code automatically but lacks narrative warmth and requires standardised docstrings on every page. (v) **screencast walkthrough** — heavier production cost, doesn't update with code changes, harder to grep, harder to keep current. Recommend (iii); it pairs naturally with #10(c) when that ships (the `/help` page can link to or embed the same manual). Sized M for initial content drop + Markdig wiring + the route page; ongoing edits per feature ship are small. **Note (post-style-guide):** `docs/STYLE-GUIDE.md` fits the same Markdig route shape — once `/manual` is wired, exposing `/style` from the same Markdig service costs ~5 minutes and gives the design system the same in-app surface as the user manual. Worth genericising the route handler so adding the next doc is a one-liner. | M |
| 12 | AI | AI-assisted genre matching | Use `IAIAssistantService.SuggestGenresAsync` to classify a book against the preset taxonomy, replacing/augmenting the rule-based `FuzzyGenreMatch` in `BulkAddViewModel.SaveBookAsync`. Higher accuracy, ~$0.0003/book on Sonnet, ~1s per book. Useful for genuinely ambiguous subject strings. | M |
| 13 | Series | Multi-series membership | A Discworld novel could belong to both "Discworld" and "Discworld: City Watch" sub-series. Currently one series per book (`Series.cs`). | M |
| 14 | Series | Support non-integer / hierarchical `SeriesOrder` | Current model is `int? Work.SeriesOrder`, which can't represent "5.5" interquels (Stormlight Archive #4.5 — *Edgedancer*) or potential "2.1 = series 2 book 1" hierarchies cleanly. The Open Library lookup parser preserves the raw publisher string and surfaces it in the suggestion message, but leaves `SeriesOrder` null when non-integer. Schema option: keep `int? SeriesOrder` for gap-detection sort + add `string? SeriesOrderDisplay` for the original publisher string. UI shows the display string; gap detection (`SharedParsers.cs:238`) still works on the int. **Defer until** capture surfaces enough cases to inform the design — many series may not need it. | M |
| 15 | UI / UX | Testing approach — slice (b) Playwright e2e | Slices (a) bUnit markup and (c) EF SQL translation are shipped — see Shipped row. (b) Playwright e2e is the third leg: end-to-end browser-driven coverage of the user flows that bUnit + integration tests can't reach (add-book scan-and-save, bulk-add multi-row capture, the chip-picker arc end-to-end, merge-author dialog flows). Playwright .NET is the standard tool. **Defer until a flow surfaces a regression that the existing test layers can't catch** — manual deploy-smoke covers most of what e2e would automate, and Playwright is the heaviest test-infra carry of the three slices. | M |
| 16 | Series | Strip trailing volume numbers from API series names | When `BookLookupService.ParseOpenLibrarySeries` (in `SharedParsers.cs`) gets a series string like `"The Destroyer #34"` it returns the whole string as the series name, polluting the suggestion match against the local `"The Destroyer"` series and producing a useless single-volume match per book. Add a regex strip of trailing `\s*#?\d+(\.\d+)?$` patterns from the series name and capture the stripped number into `SeriesOrder` if not already set. Test cases: `"The Destroyer #34"` → name `"The Destroyer"` + order `34`; `"Discworld -- 5"` already handled correctly by the existing parser. Sized S; defer until the surgical Series-ordering fix has bedded in. | S |
| 52 | Data model / API | Lookup-side contributor role detection (next-up before reference capture) | The `MudContributorPicker` shipped in PR 2d (#290, Phase F of the WorkAuthor Role cutover) accepts editor / translator / illustrator / etc. credits but is empty on every API hit — `BookLookupResult` only carries a single flat `Author` string, so the user has to type non-Author contributors by hand. Worth wiring up before reference capture starts because that's exactly where the picker earns its keep (Oxford Companion to X, OUP reference series, translated classics). **Provider coverage:** (a) **Open Library** exposes role-tagged contributors via `authors[].type` on `/works/{olid}.json` (values like `/type/author_role` pointing at "author" / "editor" / "translator" / etc.) AND via the `contributors[]` array on `/books/{isbn}.json` (each entry has `name` + `role` strings). Patchy — well-populated on classics and reference works, blank on most pulp; do an investigation sample first like #24 did with TOC fields. (b) **Google Books** `volumeInfo.authors` is just strings, no role; skip. (c) **Trove** `contributor` is just strings, no role; skip. **Implementation shape:** extend `BookLookupResult` with `IReadOnlyList<(string Name, AuthorRole Role)> Contributors` (default empty so Google/Trove paths and existing tests stay compiling); add an OL parser that maps the role-string vocabulary to `AuthorRole` (e.g. "editor" → `Editor`, "introduction" → `Foreword`, anything unknown → ignore rather than default to Contributor — silent miscategorisation is worse than missing); surface on the Add Book page by pre-populating the new picker's `Contributors` list when the lookup returns any. Bulk Add stays Author-only (per the Phase F call) — non-Author roles for bulk-captured rows still happen on Book Detail post-save. **Suggestion-message UX:** mirror the series-suggestion banner — "Found 2 editors: Doug Mauss, Jane Smith. Accept?" so the user opts in rather than getting silent auto-fill. Surfaced 2026-05-22 in the Phase F follow-up conversation. | S–M (after a coverage-sampling pass) |
| 18 | Meta | `scale-audit` skill | Follow-on audit skill reusing the chassis from the now-shipped `security-audit` skill: N+1 queries, unbounded `ToListAsync`, missing `Include`, index coverage (FKs, filtered-unique ISBN), `DbContextFactory` discipline (flag any `AddDbContext` regression or injected `DbContext`), Azure SQL Basic tier headroom vs observed query patterns. Enforces the 3000+ copies performance rule as a first-class audit. | M |
| 24 | UI / UX | Multi-work collections — TOC auto-fill (deferred after 2026-05-08 API investigation) | **Original premise:** when the user toggles Collection mode and runs an ISBN lookup, parse upstream TOC fields into pre-populated `CollectionWorks` rows so a 12-story compendium becomes 12 pre-filled rows the user just sanity-checks before save. **Investigation 2026-05-08** with a 6-book sample (Bachman Books, Inside the Funhouse, Christie Crime Collection, Complete Shakespeare, Lovecraft Tales of Horror, 2000AD Issue) found the planned shape isn't viable. Findings: (a) Open Library's `table_of_contents` field exists in the schema but was blank on all 4 `/works/{olid}.json` records checked; (b) the only place TOC-shaped data actually lives is the unstructured `description` field, and only when a contributor hand-typed it — Shakespeare's was excellent (`PLAYS (37) / POEMS (7)` headers, one title per line, even Markdown links to other Works), Lovecraft mentioned 3 stories in marketing prose ("*Call of Cthulhu*" etc.), Funhouse and 2000AD had nothing; (c) Google Books `volumeInfo.tableOfContents` was absent in every response — partial TOC sometimes leaked into `description` or `searchInfo.textSnippet` (Christie textSnippet ran three novel titles together). Effective hit rate: ~1/6 with usable structured-ish data, ~3/6 with partial natural-language TOC, 2/6 with nothing. Doesn't justify the parser + UI + AI cost as planned. **Three paths forward when revisiting:** (i) pivot to AI-extract from `description` + `textSnippet` via Sonnet (~$0.0003/book, handles Shakespeare/Lovecraft cleanly, partial coverage); (ii) build a hand-typed TOC editor on `/books/{id}` (not API-driven; user pastes/types contents once, sticks; would live as a new modal alongside the existing Add Work / Edit Work dialogs); (iii) wait for upstream data quality to improve (probably never; OL contributors aren't filling `table_of_contents`). **Useful by-products of the investigation** worth flagging as capture-time gotchas: ISBN `9780451178121` resolves to *True Crime Stories* by Max Haines on Open Library, NOT *The Bachman Books* — find a different printing if recapturing; Christie `0600766012` isn't in OL at all (Google Books has the title under `0600766128` instead); 2000AD `9772399777023` is the Issue 51 *Judge Dredd: Dark Justice* printing, not Issue 01. **Status:** deferred. Consider re-opening only if (i) a richer signal arrives (a different provider, or OL's TOC field starts being populated), or (ii) the hand-typed-TOC modal becomes worth doing on its own. | M (path-dependent on revisit) |
| 28 | UI / UX | Consolidate /shopping into /bookshop (deprecate the duplicate) | Once the bookshop arc lands (PRs 3–5), `/shopping` and `/bookshop` overlap noticeably. **Card 1 ("Do I have this?")** is fully duplicated by `/bookshop`'s scan + manual-ISBN flow — straight redundancy, only kept temporarily because /bookshop didn't exist when /shopping shipped. **Card 2 (Series gaps)** is derivable from a slightly fatter catalog snapshot — add `seriesId` / `seriesOrder` / `expectedCount` to `BookSnapshot` (or precompute gaps server-side and ship a `seriesGaps` array on `CatalogSnapshot`), drop a new IndexedDB `series` store, render as a third bookshop tab. Read-only, fits offline. **Card 3 (Wishlist)** is read-mostly but write-sometimes: the read path can go offline (extend the snapshot DTO with wishlist rows or add a `/api/wishlist-snapshot` parallel endpoint), but add / remove / "bought" actions still require an online roundtrip — same pattern as the existing "Open in app" deep-link. Three sub-PRs: (a) drop Card 1 from /shopping (XS); (b) Series gaps as bookshop tab + snapshot DTO extension (M); (c) Wishlist as bookshop tab with online-required action labels (M). When all three are in, redirect `/shopping` → `/bookshop` and remove the page. **Defer until after PR 5** so the bookshop v1 stabilises in real use first; some of the consolidation choices may shift after Drew has actually used /bookshop in a real bookshop. | M |
| 19 | UI / UX | Accessibility review | Screen reader support, keyboard nav, ARIA labels, colour contrast, focus management. Broad sweep across every page. | L |
| 20 | App | i18n + l10n support | Localise user-facing strings via .NET resource files (`.resx`) + culture-aware formatting for dates / numbers / ordinals. Currently English-only; touches every Razor page, every dialog, and any future email/notification surface. Architectural calls to make first: which cultures to support (`en-AU` minimum given Drew's location, then `en-US`, `en-GB`); date format (`DD/MM/YYYY` vs `MM/DD/YYYY` — `DateOnly.ToString` becomes culture-aware); decimal separators; pluralisation library (`IStringLocalizer` built-in, or Humanizer for richer cases); RTL support stub even if no RTL culture ships immediately. Cheaper to start early than retrofit; flagging now while the surface area is still tractable. | L |
| 22 | Meta | Claude-driven PR review for external contributions | Experiment extension. Once there's actual external interest, set up a GitHub Action that runs Claude Code on every non-author PR: Claude reviews the change, posts review comments + suggested rewrites, tags the human for the accept/reject decision. Continues the "every line of code goes through a Claude session" frame to external work too. **Blocked:** needs a collaborator or external PR traffic that justifies the setup cost + clarity on CI cost + review-quality bounds. | L |
| 49 | Mobile | Bookshelf in-app "Reset cache" button | Add a MainPage affordance (overflow menu, or long-press on Load catalog, or a button on the cache stats panel) that calls a new `ICatalogCache.WipeAsync()` — blows away the SQLite file + covers directory + meta keys, leaves MSAL tokens + Android camera permissions intact. Next Load catalog then goes through the no-watermark → full `PopulateAsync` path. Reason: the obvious user fallback (Android Settings → Apps → Bookshelf → Clear data) crashes the app on subsequent launch (see runbook memory `runbook_bookshelf_clear_data_kills_app.md`). Today's only recovery is uninstall + reinstall. An in-app reset bypasses that whole flow. UX surface: probably an overflow-menu item with a confirm dialog ("Reset local cache? You'll need to re-download the full catalogue on next Load."). Surfaced 2026-05-14 when recovering from the version-mismatch-refetch bug (see fix PR #?). | S |
| 50 | Mobile | Investigate Bookshelf crash after Android Clear data | Companion to #49: capture the actual failure mode so the in-app reset doesn't replicate it. Reproduce: install Bookshelf, sign in, load catalog, Android Settings → Apps → Bookshelf → Storage → Clear data, re-launch. Capture `adb logcat \| findstr /i "AndroidRuntime FATAL Bookshelf"` during the launch. Suspected culprits: MSAL's token-cache directory being absent vs corrupted (different SDK behaviour); `BuildInfo.DisplayString` initialisation reading an embedded resource that Clear data invalidates; some required private-storage path the app expects pre-existing. Outcome of the investigation feeds into #49's `WipeAsync()` implementation — whatever Clear data wipes that breaks the app, `WipeAsync()` must not touch. Sized XS once the logcat is in hand. | XS (after logcat capture) |
| 51 | Data model | Reference book capture — data-model nuances | Surfaced 2026-05-15 when Drew flagged reference books as the next capture wave after fiction. Genre tree already covers it (`Reference` parent with Dictionaries / Encyclopedias / Atlases / Field Guides / Style Guides / Language Learning in `GenreSeed.cs`), `BookCategory.NonFiction` and the format/series shapes work as-is, so single-volume single-author references can be captured today without schema change. Five real nuances surface at scale: (1) **No editor role on `WorkAuthor`** — `Order` exists but no `Role` enum, so edited references (Oxford Companion to X, encyclopaedias, anthologies, many cookbooks) get credited as authors and conflate with the genuine author rollup; cheap migration to add `Role` later but backfill is a question — which existing "authors" are actually editors? (2) **No `EditionNumber` on `Edition`** — *Joy of Cooking 1975 vs 2019* is materially different content; today this lives in `Work.Subtitle` or is implicit in `Edition.DatePrinted`; tiny migration to add `int? EditionNumber` later but re-keying captured rows is a sunk cost if you decide post-mass-capture. (3) **`BookStatus` is meaningless for references** — Read/Reading/Unread don't describe a dictionary. Convention (everything reference stays `Unread`) works today; heavier path adds `BookStatus.Reference` to opt the row out of the read-state UI. (4) **Multi-volume sets** (24-volume Britannica) — workable today as `Series(SeriesType=Series, ExpectedCount=24)` with one Work per volume, but per-volume titles ("Britannica — Vol. 14: Lib–Mel") are clunky; could promote to a first-class "Set" / "boxed set" concept later. (5) **Corporate authors** (OUP, Britannica as credited "author") — populate `Author` table with non-human rows that don't fit `CanonicalAuthorId` (no pen-name aliases for an institution); minor wart only. **Status:** defer the planning session until Drew has finished capturing fiction so the priority order is informed by real reference-capture pain (which nuance bites first?). Stopgaps in the meantime: editor credit → put name in `Author` with a Subtitle note; edition number → put "(3rd ed.)" in `Work.Subtitle`. | M (after planning) |
| 47 | Data model | eBook format + import from Drew's existing digital collection | New `BookFormat.Ebook` (and possibly `Audiobook`) to capture digital titles alongside physical ones. **Needs a planning session before any implementation** — significant unknowns: (1) **Data model:** does an eBook reuse the `Book → Edition → Copy` shape with a new `BookFormat` enum value, knowing Copy's `Condition` / `DateAcquired` become semi-nonsense for digital? Or split into a parallel entity (a "Holding" record alongside Copy) so the physical model stays clean? (2) **Source(s) to import from:** Drew's collection spans (at minimum, expected) Kindle library, Audible, and possibly Calibre / Apple Books — each has a different export shape and none have a stable public API. Likely candidates: Kindle CSV export from `read.amazon.com/kindle-library`, Audible's library page (no official export → scrape or manual), Calibre OPDS feed if Drew uses it. (3) **Identity:** Kindle uses ASIN, not ISBN — does `Edition.Isbn` get a sibling `AsIn`, or a polymorphic `ExternalId` table? (4) **Reading state:** physical Status is unread/reading/read; for digital we have richer signals (last-read position, %, time spent). Capture them, or stay at the same three statuses? (5) **Dedup with physical:** owning both the hardback AND Kindle copy of *Foundation* — one Book with two Editions/Holdings, or two separate Books? (6) **UI:** Library page filter by format? Separate "Digital" section, or interleaved with a small badge? Bookshelf cover thumbs already cope (the cover image is format-agnostic) but Add Book's Copy/Condition fields don't fit digital. (7) **Import scope:** one-shot bulk import vs ongoing sync — Kindle CSV is a snapshot; if Drew buys more, he re-runs the import? Or background polling? **Status:** placeholder for planning. Raised 2026-05-14. | L (after planning) |
| 23 | AI / Infra | Re-add Microsoft Foundry (Claude on Azure) | Brings back `claude-sonnet-4-6` + `claude-opus-4-7` deployments, the Foundry Private Endpoint, and the `cognitiveservices.azure.com` DNS zone. **Blocked:** current subscription is Sponsored, which MS excludes from Claude on Foundry. Needs EA / MCA-E migration first. Direct Anthropic API works in the meantime. | XL |

### Notes on Open items

- **Duplicate detection** is conservative (exact-after-normalisation). If the noise/signal ratio is wrong in practice, add fuzzy matching (Levenshtein / token similarity) as a follow-up — don't plumb confidence scores until there's a reason for it.
- **MudBlazor rollout** is ongoing via "convert as we touch" (see `ARCHITECTURE.md` > UI component library). Not a scheduled TODO — each page converts when it's substantively edited for other reasons.

## Shipped

| # | Category | Name | Description | Estimate | Actual |
|---|---|---|---|---|---|
| — | Infra / Safety | Replace migrate-on-startup with deploy-time migrations (TODO #21) | Genre-restructure deploy (PRs #271, #272) burned an afternoon on a partial-state failure that migrate-on-startup made painful to debug. Tactical fix landed in [#273](https://github.com/N3rdage/the-library/pull/273) (idempotent migration via `IF NOT EXISTS BY Id` guards). Structural fix landed in two PRs: [#275](https://github.com/N3rdage/the-library/pull/275) (PR A) introduced the `dotnet ef migrations bundle` flow — `deploy.yml` now applies pending migrations against `booktracker-staging` via AAD-auth behind a temp firewall rule before the slot deploy, and `swap.yml` does the same against `booktracker` before the slot swap; `Program.cs`'s `RunMigrationsAsync` is gated on `IsDevelopment` so `dotnet watch` keeps its convenience locally. PR B (this PR) dropped `db_ddladmin` from the runtime managed identities (`infra/deploy.ps1` now grants `db_datareader/writer` only, plus an idempotent `IS_ROLEMEMBER` revoke for existing grants), granted the GitHub OIDC SP `db_ddladmin` scoped to CI runs, and retired the `SECURITY-AUDIT.md` §10 + `audit-rules/security.md` AZRE-003 suppressions. Three wins: distinct failure signals (a migration crash is a red CI step, not a 30-minute slot-swap warmup timeout), single-instance assumption preserved as a *choice* not a one-way door (multi-instance scale-out is no longer a race against `__EFMigrationsLock`), and smaller runtime blast radius (runtime MI shed `db_ddladmin`). Blog post: [migrate-on-startup turned a 10-line SQL goof into a 30-minute deploy](https://github.com/N3rdage/the-library/blob/main/blog/2026-05-18-01-migrate-on-startup-30-minute-deploy.md). | L | 3 PRs (#273 tactical + #275 PR A + this PR PR B) + 1 hotfix (#277 dotnet-ef install) + 1 blog (#276) |
| — | UI / UX | Add Book + Bulk Add MudBlazor reskin (TODO #45 + bundled child forms + Bulk Add) | Drew's 2026-05-12 testing-feedback follow-up. `/books/add` was part-Bootstrap (outer `EditForm`, `.card`, `.btn`, `.form-check form-switch` toggles, `.alert` panels) and part-MudBlazor (embedded `MudGenrePicker`, `MudAuthorPicker`) — the hybrid read as two stacked design languages on mobile. Ported the page's chrome to Mud (`MudCard`/`MudCardHeader`/`MudCardContent` for sections, `MudSwitch<bool>` for the three toggles, `MudAlert` for lookup messages + ExistingBook warning + series suggestion + save-error states, `MudGrid`/`MudItem` for layout, `MudButton`/`MudIconButton` for actions, `MudPaper` for the collection-mode per-row builders, `MudChip` for the "Existing work" badge, `MudImage` for inline cover thumbnails). **Scope expanded** during planning to bundle the shared child forms (`BookForm.razor`, `WorkForm.razor`, `EditionCopyForm.razor`) and Bulk Add — leaving them Bootstrap would have meant Mud-inside-Bootstrap on `/books/bulk-add` (same problem #45 was fixing). EditForm + DataAnnotationsValidator preserved; MudTextField + MudSelect + MudNumericField surface validation errors via `For="..."` expressions on each field. Publisher datalist replaced with MudAutocomplete over `EditionFormViewModel.ExistingPublishers` (free text falls through for find-or-create). BulkAdd's tabular grid kept HTML `<table>` for density (MudTable's per-row API would have been a larger restructure for marginal gain), but every adjacent surface — scanner cards, photo capture, status chips, action buttons, the "Discovered" header — converted to Mud equivalents. JS interop contracts preserved verbatim: `data-isbn-lookup-input` wrapper on the ISBN input, `data-collection-work-title="@index"` on each collection row's title autocomplete, so `book-form-keys.js` continues to route Enter the same way. | S–M | 1 PR (this PR) |
| — | UI / UX | Add Book resilience — Enter-on-ISBN + graceful save errors (TODOs #43 + #44) | Drew's 2026-05-12 testing-feedback pair. **#43:** Enter inside the ISBN lookup input was submitting the surrounding `EditForm` instead of triggering `LookupAsync`. Wrapped the ISBN `InputText` in a `data-isbn-lookup-input` marker div and extended the existing capture-phase document-level keydown listener with a new branch: Enter inside the marker calls `[JSInvokable] OnIsbnLookupEnter` (which forwards to `LookupAsync`) and preventDefaults. Same pattern the collection-mode title rows already use; the listener now handles three use sites (collection rows + AddWorkDialog + ISBN), so renamed `wwwroot/js/collection-works.js` → `book-form-keys.js` and the global `window.collectionWorks` → `window.bookFormKeys` per the per-TODO note about generalising past the second use site. **#44:** `VM.SaveAsync` throws on validation problems (e.g. submitting with no authors picked, which was previously the inevitable consequence of #43); the unhandled exception was bubbling to Blazor's framework error UI and killing the page along with the user's typed input. Wrapped the page-level `SaveAsync` handler in try/catch — `InvalidOperationException` (deliberate VM throws with user-facing messages) renders verbatim in a new `alert-danger` above the Save button; any other exception type logs via `ILogger<Add>` and shows a generic "Save failed — your input is preserved" message. `/edit` sanity-check from the TODO was moot — that page was decommissioned in PR #245 just before. Test added asserting the non-collection-mode "no authors" path throws `InvalidOperationException` with a user-facing message (locks the contract the page's catch relies on). | XS + S | 1 PR (this PR — bundled) |
| — | UI / UX | Add/View/Edit polish arc — `/edit` decommission (TODO #26) | Six-PR arc that consolidated capture + browse + edit on the View page (`/books/{id}`) and retired `/books/{id}/edit`. **PR 1** — Status chip picker on View was visually broken (clicks didn't shift colour); `MudChipSet` now drives variant + theme-coloured chip per selected status with bold weight. **PR 2** — Unified Genre picker: replaced the Bootstrap `GenrePicker` with `MudGenrePicker` on both Add Book and the Work edit dialog so the same chips/typeahead surface appears everywhere; dropped the "Browse all Genres" expander; added per-Work scope labelling on `WorkEditDialog`. **PR 3** — Collection-mode Genre toggle ("Same genre(s) for all works") mirroring the just-shipped Single-Author pattern, with per-row pickers, inheritance on `AddCollectionWorkRow`, and bidirectional toggle propagation (OFF→ON = union to shared, ON→OFF = broadcast to rows) so flicking the toggle preserves picks. **PR 4** — `AddExistingWorkDialog` on Book Detail with title typeahead via `IWorkSearchService` ("Title (year) — Author · in N books") and re-attach guard; surfaced the Blazor dialog VM lifetime trap (`@inject` of a `Transient` VM hands a fresh instance, not the page's — fix: pass via `DialogParameters`). **PR 5** — Keyboard-first existing-Work autocomplete on Add Book collection rows: each row's title becomes a `MudAutocomplete` that surfaces existing matches as you type; Down + Enter attaches, free-text Enter captures as new. Mixed-mode save path attaches existing rows and creates new ones in the user-entered order; `SingleAuthor` / `SingleGenre` toggles skip attach rows; collection toggle confirms before discarding attachments. JS interop (`collection-works.js`) at the capture phase blocks MudAutocomplete's auto-pick of the first-highlighted suggestion unless the user has explicitly arrow-keyed. Known papercut: stopImmediatePropagation leaves the popover open after free-text Enter (three close-it-for-them attempts — `input.blur()`, synthetic Escape, direct `.mud-popover-open` removal — all failed because the popover is portaled and Blazor's event delegation filters synthetic events; user dismisses with Esc). **PR 6** (this row) — Decommissioned `/edit` entirely: `AddWorkDialog` on View combines attach-existing + create-new under a single entry point ("Add, but allow me to select existing if one exists"); Delete Book affordance with `ShowMessageBoxAsync` confirm; Remove-from-book on multi-Work cards (detach-or-delete based on whether the Work has other Books); rewired six surfaces that linked to `/books/{id}/edit` (Add Book duplicate-banner, Shopping result + series gaps + post-bought nav, Duplicates Book/Edition side + MergeBook/MergeEdition references, Series/Edit per-book badges); deleted `Edit.razor` + `BookEditViewModel` + the superseded `AddExistingWorkDialog`. Net diff: ~+730 / −1180 LOC across six PRs. | M | 6 PRs (#239 PR 1, #240 PR 2, #241 PR 3, #242 PR 4, #244 PR 5, this PR PR 6) |
| — | Mobile | Bookshelf enhanced Book card + espresso icon refresh (Arc D) | Two parallel changes in one PR — both Bookshelf-only, both pure-visual/pure-additive on the existing snapshot pipeline. **Enhanced Book card** — ScanPage's FoundFrame gains two always-shown-conditionally sections: **Works** (visible when the Book has more than one Work; lists "Title — PrimaryAuthor" per Work so a compendium's contents are visible at a glance) and **Editions** (visible when the Book has more than one Edition; per-edition row with a 40x60 cover thumb + format badge + ISBN so Drew can spot "I already own this exact paperback" while shopping). Server side: `BookSnapshot` gained nested `Editions: IReadOnlyList<EditionSnapshot>?` and `Works: IReadOnlyList<WorkSnapshot>?` fields with sensible nullable defaults for back-compat; `CatalogSnapshotService` projects them with per-Edition (Id/Isbn/Format/CoverUrl) and per-Work (Id/Title/PrimaryAuthor — the lowest-Order WorkAuthor on THAT Work, distinct from the Book-level rollup) detail. Cache side: new `CachedBookEdition` + `CachedBookWork` tables (PK = server Id, indexed BookId), `PopulateAsync` and `ApplyDeltaAsync` both wipe-and-rewrite per Book, `DeletedIds` tombstones cascade-remove from both tables alongside the existing `book_isbns` cleanup. New `ICatalogCache.GetBookEnrichedDetailAsync(bookId)` returns a `BookEnrichedDetail(Editions, Works)` record; ScanPage's LookupAsync fires this off in parallel with the cover fetch so the basic card renders without waiting on the secondary tables. Per-Edition cover thumb fetched via the same named HttpClient as the main cover but bypasses the disk cache (in-memory only — they're best-effort gravy, no need for the {bookId}.jpg slot which the main cover owns). **Icon refresh** — Bookshelf launcher icon background swapped from Material You purple (`#6750A4`, matching the Bookcase PWA brand mark) to espresso (`#3E2723`, matching the in-app header banner), with the spine recoloured brass (`#A67B3A`) and text lines parchment (`#F2EADB`) for palette cohesion with the running app. csproj `MauiIcon` + `MauiSplashScreen` Color attributes updated to match. Differentiates Bookshelf from Bookcase on a phone with both installed — was "two identical purple icons" before, now reads as "Bookshelf is the dark/leather mobile one". Two new server snapshot tests (Editions projected with Format+CoverUrl; Works with per-Work PrimaryAuthor on compendiums); four new cache tests (read-back; null book → null detail; back-compat null fields → empty inner lists; ApplyDelta replaces editions/works; DeletedIds cascades). 449/449 non-E2E + 44/44 cache tests green. | M | 1 PR (this PR) |
| — | Mobile | Bookshelf title search + Series gaps (TODOs #41 + #35b) | Two parallel Bookshelf features in one PR — both pure-client (no server / API / migration changes), both consume snapshot fields that already exist. **Title search (#41)** — new `ICatalogCache.SearchBooksByTitleAsync(query, limit)` mirroring `SearchAuthorsAsync`'s shape. Indexed `CachedBook.TitleLower` column populated on both `PopulateAsync` and `ApplyDeltaAsync` (the upsert path; a delta with a title change needs to re-stamp the search column or the row falls out of sync — covered by regression test). `TitleSearchPage` debounced 250ms / 2-char-min mirroring `AuthorSearchPage`. Result rows show cover + title + author + rating-stars + status badge; no tap-through in v1 — the row itself answers the "do I own this?" intent. **Series gaps (#35b)** — new `ICatalogCache.GetSeriesGapsAsync()` returning `SeriesGap` records (SeriesId/Name/Type/ExpectedCount/OwnedCount/MissingOrders). Pure-client compute: walk Series with non-null ExpectedCount, bucket Books by SeriesId, derive missing 1..ExpectedCount slots. Skip rules: no ExpectedCount → no notion of complete; zero owned → user hasn't started; no gaps → complete (all noise to surface). `SeriesGapsPage` lists each result as "{name} · {owned} of {expected} owned · Missing {orders}", with run-collapsing on consecutive missing orders (`#2-#4` rather than `#2, #3, #4`) so a way-behind series doesn't wall-of-text. MainPage gains two new leather CTAs (Find by title + Series gaps), pushing the button count from 4 to 6 — accepted noise per Drew's "won't know how it feels till I try it" call; refactor to a grouped Search/Browse menu if it gets crowded. 12 new cache tests (substring case-insensitive, alpha sort, limit respected, delta updates TitleLower, gaps skip-no-expected / skip-zero-owned / skip-complete / null-order-bookkeeping / sort-by-name). | M | 1 PR (this PR) |
| — | Mobile | Catalog delta sync — server-side watermark + tombstones + Bookshelf delta-apply (TODO #33) | Three-PR arc that turned every catalog refresh from a full ~150KB reload into a delta of just the changed Books + tombstones. **PR 1 — server watermark + ?since= filter.** `Book.UpdatedAt` (DATETIME2, default `GETUTCDATE()`, indexed). New `BookUpdatedAtInterceptor` (SaveChangesInterceptor at the DbContext level) walks `ChangeTracker.Entries()` and stamps `UpdatedAt` whenever the aggregate (Book itself, Edition, Copy, Work, WorkAuthor, BookTag join, Tag rename) changes — single-site discipline replaces per-callsite remembering. Special-cased skip-nav collection changes on Book (e.g. `book.Tags.Add(tag)` leaves Book.State = Unchanged but `entry.Collections.Any(c => c.IsModified)` is true). Endpoint binds `since` as a raw string and parses with `DateTimeStyles.AdjustToUniversal | AssumeUniversal` to enforce UTC (the Minimal API DateTime binder treats no-timezone strings as Local — wrong-by-hours filtering against a UTC-stored column). `CatalogSnapshot.LatestUpdatedAt` carries the next-token watermark; Series always full-listed (was filtered to referenced — broke delta-correctness on rename). **PR 2 — soft-delete + tombstones.** `Book.DeletedAt` (DATETIME2 NULL) + filtered `IX_Books_DeletedAt`. Global EF query filter `HasQueryFilter(b => b.DeletedAt == null)` hides tombstoned rows from every normal query (Library / View / search / merge / interceptor lookups). `BookDetailViewModel.DeleteBookAsync` and `BookMergeService` (loser-removal in a merge) switched to soft-delete: cascade-hard-remove Editions+Copies+joins, then stamp `DeletedAt = UtcNow`. Husk row survives only as a tombstone source. Delta path queries `IgnoreQueryFilters().Where(DeletedAt > since)` and emits `CatalogSnapshot.DeletedIds[]`. `LatestUpdatedAt` now factors `max(Book.UpdatedAt, tombstone.DeletedAt)` so a pure-tombstone delta still advances the watermark (otherwise infinite re-fetch loop). Suppressed EF's `PossibleIncorrectRequiredNavigationWithQueryFilterInteractionWarning` — Editions are hard-removed at soft-delete time so the orphan case the warning guards against can't happen. **PR 3 — Bookshelf delta-apply (this PR).** New `ICatalogCache.ApplyDeltaAsync(snapshot)` sibling of `PopulateAsync`: per-Book `InsertOrReplace` preserving `CoverPath` when the incoming `CoverUrl` matches the existing local row (cover-cache payoff — no thumbnail re-downloads on refresh) and clearing it when `CoverUrl` changes (so `EnsureCoverCachedAsync` re-fetches from the new URL). ISBN join rows wipe-and-rewrite per Book. `DeletedIds` removes the Book + its ISBN rows (otherwise a dead book's ISBN would still resolve from the scan path). Authors + Series wipe-and-rewrite (server always full-lists). `BookCount` recomputed from `SELECT COUNT(*)` post-merge (incremental tracking would be fragile across mixed upsert+delete). `CachedMeta` gains `latestUpdatedAt` key; `CacheMeta` record surfaces `LatestUpdatedAt: DateTime?` (default null for back-compat with stores written before the field shipped). `IApiClient.GetCatalogSnapshotAsync(since)` percent-encodes the ISO 8601 `O` token. `MainPage.OnLoadCatalogClicked` decision tree: no stored watermark → full; have token + Version matches → `ApplyDeltaAsync`; have token + Version mismatch → full reset (server deploy invalidated cache shape, mirrors PWA's deploy-detection pattern). Status text reports which path ran + counts (`delta · 3 updated, 1 removed`). Eight new tests in `BookTracker.Mobile.Cache.Tests`: watermark round-trip via `PopulateAsync`+`GetMetaAsync`, CoverPath preserved on unchanged CoverUrl, CoverPath cleared on changed CoverUrl, new Book insertion, DeletedIds removes Book + ISBN rows, Authors+Series wipe-and-rewrite (discontinued rows disappear), watermark advances across `ApplyDeltaAsync`, BookCount reflects post-merge row count not incoming delta size. Pairs with #34 (cover cache survives across delta refreshes). | M | 3 PRs (#258 PR 1, #259 PR 2, this PR PR 3) |
| — | Mobile | Bookshelf cover thumbnails with local cache (TODO #34) | Premise of the original TODO row was wrong — Bookshelf displayed no covers at all (no `CoverUrl` on the cache schema, no `CoverUrl` on `BookSnapshot`, no Image controls on any page). Reframed as "add covers to Bookshelf with local caching baked in from day 1." Server side: `CatalogSnapshotService` now projects `Book.DefaultCoverArtUrl` into the snapshot; `BookSnapshot` record gained a nullable `CoverUrl` (defaulted to null for backwards compat with existing positional constructions). Mobile cache: `CachedBook` extended with `CoverUrl` (server-supplied) + `CoverPath` (local file), with `CoverPath` resetting to null on every `PopulateAsync` (lazy re-fetch after refresh — when delta sync TODO #33 lands and Populate becomes an upsert, the path can be preserved across refreshes when CoverUrl is unchanged). New `EnsureCoverCachedAsync(bookId, http, ct)` method: idempotent, short-circuits on existing on-disk file; downloads via caller-supplied HttpClient, decodes + resizes via SkiaSharp to 200px long edge JPEG (try/catch wraps `SKBitmap.Decode` because it throws `ArgumentNullException` rather than returning null on undecodable bytes), writes to `&lt;db-dir&gt;/covers/{bookId}.jpg`, persists `CoverPath`. Returns null on any failure mode (book not cached, no `CoverUrl`, network error, non-image bytes). Mobile UI: `ScanPage` result card got a two-column layout (cover thumbnail | text stack); `AuthorBooksPage` book rows got the same shape via `BuildBookCard`'s new cover slot. Both pages start with a parchment-grey placeholder + 📖 glyph and swap in the cached image when bytes land. ScanPage cancels in-flight cover fetches on the next scan so Book A's bytes don't land on Book B's card. Named `HttpClient("covers")` added in `MauiProgram.cs` (no `BaseAddress` since cover URLs are absolute external/blob URLs); `IHttpClientFactory` injected into `ScanPage` + `AuthorSearchPage` + threaded into `AuthorBooksPage` constructor. Six new unit tests in `BookTracker.Mobile.Cache.Tests` covering: download-resize-write, second-call short-circuits (zero additional HTTP), book-not-in-cache, no-CoverUrl, download failure (and recovery on next attempt with working handler), non-image response bytes. | M | 1 PR (this PR) |
| — | Mobile | Bookshelf Author lookup mode (TODO #35a) | Mobile Phase 2 PR. New `AuthorSearchPage` with debounced 2-char minimum search over `CachedBook` via `ICatalogCache.SearchAuthorsAsync`, alias rollup so typing "Bachman" finds King's works. Wired from MainPage's `FindByAuthorButton` (leather CTA). Tap-through to per-author book list with Back. Surfaced + fixed an enum-as-int JSON serialisation bug along the way (BookStatus rendered as "2"); added a global `JsonStringEnumConverter` via `ConfigureHttpJsonOptions`. Sub-piece of the multi-part TODO #35 — remaining sub-pieces (Series gaps + Wishlist) are still Open. | M | 1 PR (#233) |
| — | Mobile | Bookshelf home page UI polish (TODO #32) | `MainPage.xaml` overhauled from a flat `<Button>` + `<Label>` stack to a leather / brass / parchment / espresso laid out for thumb-reach. Espresso header banner anchored to a fixed top bar, leather Sign in / Load catalog / Find by author CTAs at 56dp height, brass-accented Scan ISBN as the in-bookshop killer at 64dp height, signed-in vs signed-out states swapped via `IsVisible` toggles, passive `CacheStatsPanel` surfacing book count + last-synced alongside the status label rather than burying them in `StatusLabel` text. Single file. | S | 1 PR (#230) |
| — | Mobile | Bookshelf app icon + splash (TODO #31) | Bespoke leather/parchment-themed SVGs at `BookTracker.Mobile/Resources/AppIcon/appicon.svg` (background, solid Material-You purple #6750A4 for visual continuity with the Web PWA's brand mark) + `appiconfg.svg` (foreground, spine + text-lines design mirroring the PWA's `wwwroot/icons/icon.svg`) plus a `Resources/Splash/splash.svg`. Wired into csproj via `<MauiIcon>` + `<MauiSplashScreen>`. Adaptive-icon foreground / background pair handles Android's round / squircle launcher constraints cleanly. | S | 1 PR (#229) |
| — | Mobile | Bookshelf letterbox scan UX — green target overlay (TODO #30) | The `ScanPage` camera viewfinder previously filled its container with no visual guide; added a four-BoxView letterbox (`Color="#B3000000"`) around a transparent middle `<Border>` with a green Stroke (`#4CAF50`, 3dp, 4dp corner radius) as the "centre the barcode here" cue. Star sizing (`1.5*,*,1.5*` / `*,6*,*`) keeps the ~3.5:1 book-barcode aspect across screen sizes; `InputTransparent="True"` on the overlay so it can't intercept camera-handler taps. Pure XAML, no code-behind change. Mirrors the Web `/bookshop` scan rectangle treatment. | S | 1 PR (#218) |
| — | Mobile | Manual ISBN entry — `+X` button for ISBN-10 check digit (TODO #40) | Drew's testing-feedback fix. Keep `Keyboard="Numeric"` as the default on `ScanPage.xaml`'s manual-entry Entry (the on-screen numeric keypad has big keys; switching to `Keyboard="Default"` would have shrunk the digit keys to satisfy the ~1% case of ISBN-10s ending in `X`). Added a small brass-outlined `+X` button between the Entry and the Lookup button — three-column Grid replaces the two-column. Tapping `+X` appends `"X"` to the entry text, re-focuses the Entry, and moves the cursor to the end so follow-up typing continues from there. The existing cleanup in `OnManualLookupClicked` already permitted `X` / `x` so no validation change. **Diagnosis lesson:** the obvious fix (`Keyboard="Default"`) optimised for the visible edge case at the cost of the unmeasured common path — same shape as the chip-picker arc trap. Drew's framing surfaced the asymmetry before I shipped the wrong fix. | XS | 1 PR (bundled with Mobile Phase 2 #35a) |
| — | UI / UX | Bookshop mode — offline-capable mobile lookup | Mobile-optimised, offline-capable lookup surface at `/bookshop`. Solves the killer mobile use cases — ISBN-scan-while-shopping (potentially in poor-reception areas) and author-lookup-on-the-go — with sub-100ms responsiveness independent of network state. Five-PR arc per [`docs/bookshop-mode-design.md`](docs/bookshop-mode-design.md). **PR 1 — `GET /api/catalog-snapshot`** Minimal API endpoint returning slim catalog JSON; `CatalogSnapshotService` projects ~150KB gzipped at the 3000-books target with proper canonical/alias author rollup; sets the `*Endpoints.cs` convention for future API surfaces. **PR 2 — service worker + IndexedDB infra:** `service-worker.js` extended with route-specific cache-first strategies for `/bookshop*` and `/api/catalog-snapshot` (existing network-first preserved elsewhere); `catalog-cache.js` module on `window.catalogCache` exposes `init` / `refresh` / `lookupByIsbn` / `lookupByAuthor` / `searchAuthors` / `getMeta`; pure JS, no Blazor dependency, console-testable. **PR 3 — `/bookshop` Scan mode:** the page itself + barcode scanner + manual ISBN + result card; introduced `BarcodeScanner.startJs` (pure-JS callback variant of the existing Blazor-DotNetObjectReference `start`); the `X-Catalog-Refresh: 1` header bypass + Refresh button got pulled forward from PR 5 because the SW's stale-while-revalidate meant fresh adds wouldn't surface without it. **PR 4 — Author lookup mode:** Scan/Author tab toggle, debounced search (250ms, 2-char minimum), tap-through to author's books with Back, alias resolution (typing "Bachman" finds King). Surfaced + fixed an enum-as-int JSON serialisation bug (BookStatus rendering as "2"); added a global `JsonStringEnumConverter` via `ConfigureHttpJsonOptions`. **PR 5 — polish:** stale-cache warning at 7 days, top-of-page offline banner with `visibilitychange` handler, auth-expired detection (401 / redirect-to-`/.auth/` / non-JSON) with sign-in CTA, plain-DOM toasts for refresh feedback. **Architecture decision deferred mid-arc:** design doc called for Static SSR mode for `/bookshop` to escape the SignalR circuit, but the project's `<Routes @rendermode="InteractiveServer">` is set globally — opting one page out is non-trivial. Pragmatic call landed: keep InteractiveServer + write the page with zero Blazor interactivity (no @onclick / @bind / @code beyond a one-shot `OnAfterRenderAsync` JS init kick). When offline the SW serves cached HTML and the JS layer keeps working unaided; the would-be circuit just doesn't connect, harmlessly. **Note on Task #12 (mobile expander click latency):** bookshop mode bypasses SignalR for its own flows but the Library page's author-expand still uses the Blazor circuit. The lazy-load cover work (separate Shipped row) addressed the dominant symptom on `/books`; whether the underlying expander mechanic is still felt-slow is a Drew testing call. Next-up arc: TODO #28 (consolidate `/shopping` into `/bookshop` — the "Do I have this?" card is now duplicated, Series gaps + Wishlist could fold into bookshop tabs). | L | 5 PRs |
| — | UI / UX | Lazy-load cover images on browsing surfaces | Surfaced by author-expand on the Library page feeling laggy when the author has many books — the page was blocking rendering the expanded list until every cover image had loaded, even though most were below the viewport. Same pattern across other cover-heavy lists. Added `loading="lazy" decoding="async"` to every cover `<img>` / `<MudImage>` on the high-traffic browsing pages: `Books/List.razor` (desktop table + mobile card), `Authors/Detail.razor`, `Publishers/Index.razor`, `Books/Detail.razor` (hero + edition cover), `Shopping/Index.razor` (multi-result + single-result), `Books/Add.razor` (candidate covers), `Books/BulkAdd.razor` (row covers). MudImage forwards unmatched HTML attributes via `UserAttributes` so the standard HTML attributes pass through onto the rendered `<img>`. Already in use on the merge dialogs under `Components/Pages/Duplicates/Merge*.razor` — this generalises the pattern. Pairs with TODO #7 (perf observability) — a future synthetic page-load smoke test on `/books` would catch regressions. | XS | 1 PR |
| — | UI / UX | Multi-work collections PR3 — chip-picker on Edit page's "Other works" + new-work form parity | Replaced the plain author `<input>` on `/books/{id}/edit`'s "Add new work" form (under "Other works in this book") with `MudAuthorPicker` — chips, autocomplete, find-or-create. `BookEditViewModel.NewWorkAuthor` (single string) became `NewWorkAuthors` (List<string>); `AddOtherWorkAsync` resolves all chips via `AuthorResolver.FindOrCreateAllAsync` (same helper Primary Work uses), attaches via `AssignAuthors`, formats the OtherWorks list display through `WorkAuthorshipFormatter`. Mobile-relevant — co-authored stories in compendiums become enterable without going through Add. | S | 1 PR (#196) |
| — | Observability | Display deployed version (commit SHA) in UI footer | `BuildInfo` service reads `AssemblyInformationalVersionAttribute` and exposes `ShortSha` + `CommitUrl`; `MainLayout.razor` footer renders a muted "- Version: a1b2c3d" linking to the GitHub commit when present. SHA injected at build via `dotnet publish /p:SourceRevisionId=${{ github.sha }}` in `.github/workflows/deploy.yml`. Backed by `BuildInfoTests`. Made the May 2026 deploy incident materially easier to diagnose — could see at a glance which bits were live on each slot. | S | 1 PR |
| — | UI / UX | Multi-work collections PR1 — N works per Book, no "primary" Work | Replaced the implicit "primary work" model (Library list rendered `Works.FirstOrDefault()`) with first-class multi-work treatment driven off `Book.Works.Count`. **Display:** subtitle suppressed when `Works > 1` (the inner-Work subtitle was the subtitle of an arbitrary story, reading as data noise) and a muted "N works" indicator added below the title in both desktop table and mobile card layouts. `BookListItem` gained a `WorkCount` field. **Add flow:** new "This is a collection" toggle on `/books/add` alongside the No-ISBN switch — flipping it swaps the single WorkForm for a repeatable Title + chip-picker + subtitle + first-published row builder with + / − controls. Lookup in collection mode fills only `Book.Title` + cover (the lookup describes the collection, not its constituent works); genre candidates and series suggestions are suppressed because each Work has its own. Save creates Book + N Works, dropping rows that lack a title or any authors. Author union is resolved in one pass so a name shared across rows resolves to the same Author entity (per-row `FindOrCreate` would have inserted duplicates because the existence check queries committed rows only — surfaced by the first test run). **Conversion at runtime is already supported** via `/books/{id}/edit`'s "Other works" section: any single-work book becomes a collection visually the moment a second Work is attached, no flag, no migration. PR2 (#24) auto-detects collection contents from upstream TOC fields; PR3 (#25) brings chip-picker parity into the Edit page. | M | 1 PR |
| — | UI / UX | Empty-state + loading-state audit + navbar theme refresh | Single combined polish PR triggered by an "are #9 + #10 from the chat sequence small?" check that turned up "audit-already-done" — every page already had an empty state, and the recent MudBlazor migrations were consistent on `MudProgressCircular` while the Bootstrap holdouts were internally consistent on `spinner-border` (per "convert as we touch"). Two paper-cuts addressed at the same time: (a) Authors + Publishers empty states gained "Add a book" CTA buttons matching the Library page's "Add your first book" pattern; (b) MainLayout navbar refreshed to the leather/brass palette via a small `.navbar-themed` CSS layer in site.css — espresso `#3E2723` background, parchment `#F2EADB` link text, brass `#A67B3A` for the active route. Bootstrap navbar structure (data-bs-toggle hamburger collapse at lg breakpoint) stays; only the visual changes. Five files: site.css, MainLayout.razor, Authors/Index.razor, Publishers/Index.razor, this row. | S | 1 PR |
| — | Series | Multiple authors on anthologies | Closed without code. The Work refactor (shipped earlier as the multi-author cutover) already covers this row's use case. `Series.Author` is `string? Author` (nullable, display-only label); the actual authorship lives on `Work.WorkAuthors` (M:N, ordered) — so for an anthology like "The Best Science Fiction of the Year" each volume is its own Book → Work with its own per-volume editor (Dozois one year, Strahan another), and `Series.Author` carries either null (UX renders "Various / unspecified" via `Series/List.razor:85`) or a free-text "Various editors" label. Edit form already flags Author as `placeholder="Optional"` (`Series/Edit.razor:50`). Same close shape as the earlier Story-vs-Work row: a model decision made for one reason resolves another use case incidentally. | M | (no code) |
| — | UI / UX | Bulk Add hydrates from local library on duplicate ISBN + OCR ISBN-X prompt fix | Two-PR resolution of the original "Bulk Add rejects ISBN-10s ending in `X`" TODO. **PR1 (#173)** — tightened the OCR prompt so GPT-4o / Claude reliably return the trailing `X` check digit (defensive cleanup; turned out not to be the actual root cause). **PR2 (this fix)** — Bulk Add was unconditionally calling `LookupByIsbnAsync` for every ISBN, including ones already in the local library. The trigger case was `055210617X` (*Funny Money* by Richard Sapir) — already on Drew's shelf, but not indexed by Open Library / Google Books / Trove — so the row rendered as "Unknown book" despite the local hit. Single Add Book had no such bug because it short-circuits on local-library hits before calling upstream. Fix: replaced `CheckDuplicateAsync` (which only set `IsDuplicate=true`) with `TryHydrateFromExistingAsync`, which loads the existing Edition + Book + Works + WorkAuthors + Publisher and populates the row from local data when the ISBN is already owned, then short-circuits the upstream lookup. Source field reads "Local library" so the user can tell the row came from the DB, not an API. Also surfaced two adjacent code smells worth fixing while we were there: the catch in `LookupRowAsync` was silently swallowing every exception with no logging (now `LogError`); and `seriesMatch.FindMatchAsync` was inside the same try block, so any failure flipped Status from Found back to NotFound (now wrapped in its own catch with a `LogWarning`). Regression test seeds a Pinnacle/Sapir Edition and asserts the lookup service is never called. **Diagnosis lesson** — the visible symptom ("X-suffix ISBNs fail") pointed at the wrong layer. The OCR-prompt hypothesis from the original TODO and PR #173 was a reasonable defensive fix but didn't move the symptom; the actual cause was the page-divergence on the duplicate-ISBN path, which Drew surfaced by noting the same ISBN succeeded in single Add Book. Worth remembering when a "fix" doesn't change behaviour: trust the user's repro over the original hypothesis. | S | 2 PRs (#173 OCR prompt, this PR Bulk Add hydration) |
| — | UI / UX | New-series form stays on Add screen after save | The `/series/new` and `/series/{id}` routes share `Edit.razor`. After save, `Nav.NavigateTo($"/series/{id}")` updated the URL, but Blazor's enhanced navigation reused the same component instance (only firing `OnInitializedAsync` once per lifetime), so the form stayed in its just-submitted "Create" state. Three confident-but-wrong theories shipped first (`SeriesId is null` route check, service-worker passthrough, `OnParametersSetAsync` re-init) before pivoting to `Nav.NavigateTo(..., forceLoad: true)` — the sledgehammer that bypasses the lifecycle question entirely. The four-iteration arc became the forcing function for promoting Playwright e2e (TODO #16 slice (b)) from "deferred" to "doing it now". Blog post `2026-05-06-01-going-with-the-sledgehammer.md` captures the lesson: when you've been wrong twice without observable verification, find the option that doesn't depend on your diagnosis being right rather than searching for a smarter theory. | XS | 2 PRs (#168 fix, #171 blog post) |
| — | App | Proper error handling — App Insights wiring + friendly /Error and /NotFound | Two-PR breakdown of the original M-sized TODO. **PR1 (#163)** — registered `Microsoft.ApplicationInsights.AspNetCore` SDK explicitly so `ILogger` calls flow to App Insights as `traces` with message-template properties (e.g. `{Isbn}`) as custom dimensions; the App Service codeless attach gives baseline request/dependency telemetry but not structured logger output. Added `UserTelemetryInitializer` that stamps the Easy Auth-authenticated user onto every telemetry item via `IHttpContextAccessor`. **PR2 (#166)** — rewrote `/Error` with category-mapped friendly messages (`DbUpdateException` → "Couldn't save", `HttpRequestException` → "Couldn't reach external service", default fallback) via a dependency-free static `ErrorMessageMapper`; surfaces the W3C trace ID for support quoting. New `/NotFound` page wired via `UseStatusCodePagesWithReExecute`, sharing markup with the Blazor `<Router><NotFound>` template via a `Shared/NotFoundContent.razor` component (this dual-wiring fixed a post-merge bug where the server-side re-execute briefly showed the friendly page before Blazor's client-side router fell back to the framework's blank "Not found" text). Reuses the existing `/icons/icon.svg` PWA icon for visual continuity. Both pages stick to plain HTML + Bootstrap rather than MudBlazor — defensive choice since they render when something else has already broken. Stack traces stay dev-only via the existing `!IsDevelopment` gate on `UseExceptionHandler`. Deeper categorisation (e.g. `OperationCanceledException`) deferred until production logs surface a third repeat shape. Adjacent observability follow-up (#165) added trace-on-attempt + warn-on-no-result in `BookLookupService` so the `/books not found` path is no longer silent in App Insights. | M | 2 PRs (#163 PR1 SDK wiring, #166 PR2 error pages) |
| — | Series | Series-order ordering on Author drill-down + Library Collection group | `AuthorListViewModel.LoadDetailAsync` previously sorted Works by `Title` only — for an author with a manually-numbered series (Drew set up Discworld) this buried the curated order. Same story on `BookListViewModel.LoadGroupBooksAsync` when grouping by Collection. Both now honour `Work.SeriesOrder` (with `int.MaxValue` sink for null orders) and tail standalone Works alphabetically after the series cluster on the Author view. Compendium edge case (Book whose Works span the same series at multiple orders) takes the minimum. Two regression tests added against the real-SQL Testcontainer chassis (Pratchett-shaped fixtures: Discworld + Bromeliad + standalone). Stale "currently defaults to publication order" wording on #3 corrected; the deferred non-integer modelling question stays open under #3 (overlaps with #15). New TODO #17 captured for stripping trailing `#NN` volume numbers from API series names so the suggestion match doesn't fragment. | S | 1 PR |
| — | UI / UX | Testing approach: SQL translation + bUnit markup (slices (c) + (a) of #16) | Two of the three test gaps from #16 closed. **Slice (c) — EF SQL translation:** swap `Microsoft.EntityFrameworkCore.InMemory` for `Testcontainers.MsSql` + `Respawn` so every test runs against a real SQL Server. The swap surfaced 11 latent bugs InMemory had been hiding (hardcoded IDENTITY values on Genre seeds, unique-Name conflicts on duplicate Author/Tag inserts), and going forward catches LINQ patterns that pass C# but fail SQL translation — the EF Core 10.x `/publishers` regression class. Per-test wipe via Respawn + container reuse keeps the suite at ~24s. One test skipped with documented reason (case-insensitive duplicate detection — schema can'\''t store the test scenario under SQL Server'\''s CI collation). **Slice (a) — bUnit markup:** added bUnit harness (`ComponentTestBase` + permissive JS interop + MudBlazor service wiring) with 9 tests on `MudAuthorPicker` covering chip render, the `OnCommitKey` add/dedup/trim paths, and chip × removal. Locks in the chip-picker invariants that PR #154 took six post-merge fix commits to nail down. Slice (b) Playwright e2e remains open at #16, deferred. | M | 2 PRs (#156 SQL translation, #157 bUnit markup) |
| — | Series | Multi-author Works (co-authored books) | Convert `Work.AuthorId` from a single FK to M:N via a `WorkAuthor` join entity with `Order` so co-authored works (Preston + Child writing *Relic*; Murphy + Sapir on the Destroyer series) are credited correctly. Two-PR additive-then-cutover (`patterns.md §5`): PR1 added the schema + idempotent backfill + dual-write at every save site + multi-author chip UI on Add / Bulk Add / WorkEditDialog. PR2 cut over reads everywhere (~25 files: VMs, services, Razor) + dropped `Work.AuthorId` + introduced `WorkAuthorshipFormatter` ("Preston & Child" / "Preston, Child, Pendergast"). Behaviour changes post-PR2: Authors page lists works where the author is a co-author (not just the lead); Home top-author tallies count each `WorkAuthor` row independently. Author canonical-alias rollup chains transparently through the join. **Post-merge bug arc** (PR #154) was a humbling six commits: chip-per-keystroke under MudAutocomplete `CoerceValue=true`; em-dash placeholder rendered as literal `—` text; Enter submitting the surrounding form because Blazor's `@onkeydown:preventDefault` can't be conditional on key value at runtime; comma getting inserted into the input; Enter ignoring arrow-key dropdown highlight. Final shape uses a JS-interop keydown listener that always preventDefaults Enter/comma but defers to MudAutocomplete's native pick handler when `aria-activedescendant` indicates a highlighted dropdown item. **Every one of the post-merge bugs would have been caught by browser-level UI tests** — the arc became the forcing function for promoting #16 Testing approach to actively-blocking. | M | 3 PRs (#150 PR1, #152 PR2, #154 post-merge fix arc) |
| — | Meta | `security-audit` skill (pilot for audit-skill chassis) | User-level read-only Claude Code skill that flags security issues across code, infra (Bicep), CI/CD (GH Actions), and secrets/config. Produces a dual-audience Markdown report: structured frontmatter + stable rule IDs (`SEC-001`…) + `file:line` citations for LLM consumption, plus narrative executive summary + severity grouping for human scanning. Skill files at `~/.claude/skills/security-audit/` (user-global, not in this repo). Project-specific rules at `audit-rules/security.md` at the repo root — relocated mid-pilot from `.claude/audit-rules/` after the second-stack run on `what-now` exposed that `.claude/` is gitignored by `create-next-app` defaults, which would have made suppressions silently per-machine. Templates ship for ASP.NET-Azure (`AZRE-001`–`009`) and Next.js-Supabase (`NEXT-001`–`010`). Reports land in `audits/` (gitignored). Pilot validated on two stacks (BookTracker + `what-now`); chassis pattern proved durable, unblocks `a11y-audit` (#5), `codehealth-audit` (#6), `scale-audit` (#18). Out of scope and not promoted to fresh TODOs: `extends:` / `suppress:` rule-pack syntax, `/init-audits` scaffolder, plugin packaging for cross-machine distribution — would each need their own pilot if/when adopted. Retro: `retros/retro_security_audit_skill_poc.md`. Blog post: `blog/2026-05-03-01-the-cost-of-reusable.md`. | M | 4 PRs (#143 BookTracker bootstrap, #144 retro, #145 audit-rules relocate, #146 blog) |
| — | Data model | Short stories / novellas as first-class entities | Closed without code. The Work refactor (shipped earlier) already covers the use cases this row imagined: Work IS the abstract creative unit (Title, Author, FirstPublishedDate); the many-to-many `Book ↔ Work` join handles "story appears standalone *and* in a collected-works book"; queries like "which book contains story X" or "all stories by author Y" already work via the `BookWork` join + Work fields. A separate Story entity would only earn its keep if it had something Works don't (finer-grained publication metadata, story-specific cross-references). If a use case shows up that genuinely needs Story-distinct-from-Work, write a fresh row at that point with the concrete need. | L | (no code) |
| — | Infra | Validate Trove ISBN lookup end-to-end | NLA API key arrived 2026-04-30. Validated against canonical Trove-only ISBN `9780645840407` end-to-end on dev (key in `appsettings.Development.json`) and prod (`deploy.ps1 -TroveApiKey '<key>'` → KV → `Trove__ApiKey` resolves). DTO matches Trove v3 live response (no parser drift); the existing `TroveStringValues` helper handles the array-or-string flip cleanly. Drew flagged a small number of non-Trove-non-OL-non-Google misses during real capture (books with ISBN + LCCN); discussed and concluded **manual entry is the right answer** for the long tail rather than wiring a fourth provider — LOC SRU/Z39.50 is rabbit-hole-y, WorldCat v2 is paywalled, and the existing `Add as follow-up` UX on Bulk Add already handles "scan, fill in inline, save with `follow-up` tag" gracefully. | S | (no code) |
| — | Series | Actionable series-suggestion accept (PR-B follow-up to API enrichment) | Adds an Accept button to the suggestion banner on Add Book and Bulk Add (per-row). Click → save-time find-or-create of the `Series` row (default `SeriesType.Series`) and attach the new `Work` to it with `SeriesOrder` from the upstream API number. Only renders for API-sourced reasons (`ApiMatchExisting` / `ApiMatchNewSeries`); local-fallback reasons stay informational. Two-state banner UI — pending shows Accept + dismiss; accepted shows "✓ Will attach to series: X #N" + Undo. Acceptance state clears on fresh lookup / Reset so it can't bleed across captures. Closes the bulk-capture-flow gap surfaced after PR #134's series fix went live. | S | 1 PR (#?) |
| — | Series | API enrichment for series detection (Open Library) | When ISBN lookup hits Open Library, the `series` field is parsed (e.g. `"Discworld -- 5"` → name `Discworld`, order `5`). Non-integer orders (`"5.5"`) preserve the raw publisher string and surface it in the banner while leaving `Work.SeriesOrder` null — the model concern is tracked at Open #17 (non-integer SeriesOrder). `SeriesMatchService` gained a `FindMatchAsync(BookLookupResult)` overload that prefers upstream API data and falls back to the existing local title/author matching when the lookup didn't return series info. Used by both Add Book and Bulk Add. Two new `MatchReason` values: `ApiMatchExisting` (API series matches a local Series row) and `ApiMatchNewSeries` (API series, local row not yet created — banner informs the user). Google Books series parsing was deliberately skipped — its API doesn't expose a reliable series name, only opaque IDs. **Follow-up shipped:** PR-B (actionable Accept button + save-time find-or-create) — see row above. | M | 1 PR (#?) |
| — | Series | Series-vs-Collection helper text on Series/Edit | Inline `<div class="form-text">` under the Type select on `/series/{id}` and `/series/new` explaining the distinction with examples (Series = numbered known order, Ender's Game Saga; Collection = loose grouping, Discworld / Hercule Poirot). One file, ~5 lines. The richer help-system pattern (TODO #13) tracks the deeper version. | XS | 1 PR (#?) |
| — | Infra / Security | Separate staging database from production | Prod and staging slots now talk to separate Azure SQL databases (`booktracker` + `booktracker-staging`) on the same SQL server. `DefaultConnection` is slot-sticky so swaps stay code-shaped; each slot's managed identity is granted only on its own DB; `deploy.ps1` drops orphan staging-identity grants from the prod DB to keep the isolation clean. First deploy: empty staging DB, migrate-on-startup builds the schema. **Surprise post-deploy:** Bicep redeploy + slot restart + first migrate-on-startup briefly stacked on the S1 ASP + Basic SQL (5 DTU) combo — both slots looked dead with `Connection reset by peer` during AAD login (~6min after deploy.ps1 finished), recovered after a full Stop/Start of each slot. Surfaced new TODO #12 (synthetic monitoring + capacity alerts) so the next degraded-but-self-resolving event is visible. | M | 1 PR (#129) |
| — | Security | GitHub Environment + 2-min wait timer for swap.yml | Set up the `production` GitHub Environment with a 2-minute wait timer (fat-finger cancellation window) and Deployment branches restricted to `main` only. Wired `swap.yml` via `environment: production` on the swap job. Reviewer-approval stays future, blocked on having a collaborator (TODO #22 — Claude-driven PR review for external contributions). | S | 1 PR (#124) |
| — | UI / UX | PWA `mobile-web-app-capable` meta tag | Added `<meta name="mobile-web-app-capable" content="yes">` alongside the apple-specific one in `Components/App.razor`. Clears the Chrome deprecation warning while keeping Safari install-to-home-screen working. | XS | 1 PR (#123) |
| — | UI / UX | Manage publishers page | MudBlazor page at `/publishers` mirroring `/authors` structurally — per-row drill-down to editions, inline rename, two-step-confirm merge (no alias model — outright absorption), delete-unused (guarded to zero-edition rows by the `Edition.PublisherId` Restrict cascade). `PublisherListViewModel` holds ops inline matching `AuthorListViewModel`; merge runs in an explicit transaction matching `AuthorMergeService`. | M | 1 PR (#121) |
| — | Blog | Initial blog post — "Why the most-edited part of our codebase isn't code" | First post in the AI-collaboration blog series. Tour of the `.claude-memory/` directory (feedback / user / project / reference types), argues persistent context is first-class engineering work, four rules for durable memory files. Written by Claude, reviewed + approved by Drew — matches the repo's AI-first framing. Triggered a new rule in `feedback_memory_commits.md` about when memory should land (arc-close / external reference / monthly), because 46 untracked memory files had accumulated and the post referenced retros that hadn't been pushed yet. | M | 2 PRs (#117 memory-landing prerequisite, #118 the post itself) |
| — | Meta | Plan + evaluate + flip repo to public | `GOING-PUBLIC.md` captured the decision (single public repo vs split — decided single), stranger's-eyes re-scrub of infra, and the `.claude-memory/` exposure decision (Option A — keep all tracked; workflow-preference files are a portfolio positive). Followed by README-from-scratch landing page with the experiment framing up front, then the actual visibility flip in GitHub Settings. Unblocked the GitHub Environments TODO (since shipped — see above); added a far-future TODO for Claude-driven PR review on external contributions (now #22). | S | 2 PRs (#115 planning doc, #116 README + pre-flip polish) |
| — | Security | Easy Auth client secret rotation | Scheduled twice-yearly rotation via GitHub Actions using the existing booktracker-ci OIDC SP. Keeps latest 2 passwords alive to cover KV reference refresh. 2-year secret lifetime kept as fail-safe. Granted the CI SP KV Secrets Officer, owner of Library-Patrons, Graph Application.ReadWrite.OwnedBy. | M | 1 PR (#111 — bundled feature + fix for Graph app-role grant permission/error handling) |
| — | Security | Security audit (multi-area walkthrough) | Full security posture review: git history secret scan (gitleaks), Easy Auth excludedPaths, CSP + security headers, SignalR auth, KV access paths, dependency vulns, PII in logs, SQL injection surface, JS interop XSS, Azure RBAC scoping, HSTS + HTTPS. Delivered `SECURITY-AUDIT.md` as a living doc + `.github/workflows/security-scan.yml` (gitleaks on PRs + weekly + monthly issue-opening review prompt) + baseline CSP meta tag + X-Frame-Options/X-Content-Type-Options/Referrer-Policy headers. All areas passed; one new open TODO (#3, nonce-based CSP tightening). | L | 1 PR |
| — | UI / UX | MudBlazor pilot + warm library theme | Pilot on Home + MergeBook, custom "leather/brass/parchment/espresso" palette, opt-in rollout across subsequent pages. | M | 2 PRs (#93 pilot+theme, #94 ARCHITECTURE doc) |
| — | UI / UX | Book detail (View) page arc | /books/{id} replaces /edit as the default browsing surface: read-only scaffold → inline auto-save (rating/status/notes/tags) → modal edits for Book + Work → Edition + Copy modals → Library list swap. | L | 6 PRs (#95, #97, #98, #99, #100, + early genre taxonomy #101) |
| — | UI / UX | MudBlazor genre picker | Typeahead + chips + collapsible MudTreeView; wired into WorkEditDialog. Shipped alongside non-fiction taxonomy expansion (Reference/Art/Religion). | M | 2 PRs (#101 taxonomy, #102 picker) |
| — | UI / UX | Authors page drill-down rewrite | MudBlazor rewrite of /authors with per-row Works/Books toggle, alias rollup on canonical rows, deep-link from Home top-10. | M | 2 PRs (#107 rewrite, #109 scroll-into-view polish) |
| — | UI / UX | Mobile UX polish bundle | Hamburger auto-close on nav + clickable Home stat tiles. | S | 1 PR (#106) |
| — | UI / UX | Progressive Web App | Installable on mobile + desktop via manifest + icons + service worker. Network-first-with-cache-fallback for static assets; `/_blazor/*` passes through. Icons reproducible via `scripts/generate-pwa-icons.ps1`. | M | 2 PRs (#91 install, #92 auth-exclusions fix) |
| — | Duplicates | Duplicate management arc (PRs 2–5) | Author merge, Work merge + attach-existing-Work, Edition merge + auto-fill-empties + cover retrofit, Book merge. Detection + listing predates this list. | L | 4 PRs (#87, #88, #89, #90) |
| — | Bugs | Bug nest — double-click, slot-sticky AI, shopping NRE | Dialog double-click guard; Bicep always-emit KV refs + slot-sticky; Shopping ISBN-match NRE (missing `.ThenInclude(w => w.Author)`). | S each | 3 PRs (#103, #104, #105) |
| — | AI | AI book recommendations | Multi-provider (Anthropic direct + Azure OpenAI + Microsoft Foundry in code) with runtime picker. Genre cleanup, collection cataloguing, shopping suggestions, book advisor. | L | ~17 PRs (#45–#61) |
| — | UI / UX | Wishlist UI | Integrated into Shopping page as the shopping-list section. | S | 1 PR |

### Notes on Shipped items

- Priority numbers are omitted for shipped rows — the `#` column is a placeholder. If we start planning shipped work retrospectively (e.g. for post-merge review), priority can go back in.
- The retro for each shipped arc lives under `.claude-memory/retros/` and is the canonical source for surprises/lessons beyond the one-line summary above.
